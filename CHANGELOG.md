# Changelog

> 0.8.0–0.12.x 为公开发布前的内部迭代版本，记录完整保留；首个公开发布为 v0.13.0。

## [0.13.0] - 2026-09-09

### 新功能
- **智能选路·地基(M3a)**:全网卡地址枚举(虚拟网卡过滤)；relay 注册应答回报公网出口；名片体系(生成/解析/粘贴添加，跨网段发现兜底)；连接记忆制自动重连(退避 2-30s，5 败回落)；TrustBroken 协议通知(移除信任即时断连降级)；静默期广播 15s；隐身出站探测放行
- **智能选路·探测与评分(M3b)**:通道记录+阶梯带宽探测(64KB/512KB/4MB)+RTT Ping；评分选路(RTT50%/带宽35%/稳定15%，迟滞≥15 防横跳)；退化切换(RTT 翻倍触发，新传输走上通道)
- **智能选路·呈现(M3c)**:通道标签(直连/经中继/未知，双端)；通道面板(每地址明细+重测)；强制走中继开关(排障后门，per 设备持久化)
- **设备页收尾**:卡片独立「推送」按钮(直开向导步2)；本机 IP 徽章升级为名片一键复制；手动添加支持粘贴名片全文
- **Kotlin 传输页 v0.12 对齐(M2)**:FFI TransferDto 契约扩展+卡片状态机(七态/仲裁/终态吸收)+两级删除+磁盘历史+并发队列+父子卡片 UI+单进度条

### 打磨
- **R1 单进度条**:传输卡只保留一个进度条(用户定案修订)，remote_done 转内部健康信号
- **R2 速度平滑**:3s 滑窗 done 差分+EMA(τ=2s)+3× 尖峰封顶+500ms 显示节流；ETA 随动
- **配对健壮性(P2)**:九分支失败矩阵梳理；错码分层反馈(还可重试 N 次)；冷却倒计时+设备卡徽章；超时带可操作建议
- **通知深链热启动修复**:pendingTab 改 StateFlow，前台点通知即切页
- **远程重命名移植**:Compose 重构时缺失的重命名菜单恢复
- **手机远程多选**:多选底栏可见性修复(LazyColumn weight)+全选+批量下载
- **信任列表缓存**:配对/移除后 Settings 页即时刷新
- **重复设备条目**:同广播名旧离线条目视图过滤(24h+)
- **连接记忆制**:启动/断线自动重连(退避 2-30s，5 败回落)；配对成功自动连接
- **性能基线落档**:吞吐/批量/渲染/空闲足迹(docs/audit/perf-baseline.md)

### 修复
- **单文件推送三连修**(真机四轮探针验证):小文件全秒传跳过补发 Started 事件配对(此前占位卡永卡 pending/total=0);source job 被动绑回占位卡消"一次推送 done×2 双卡";engine_id 锚定 offer job 使暂停/取消经 T16 控制桥级联;成功路径 select 退出释放并发许可(此前泄漏致后续推送永久排队);plain push_files 接通取消信号
- **FFI pull_files 多选**:此前两分支同值只拉第一个文件——改逐文件顺序拉取聚合单卡(ID 恒定/整批终态)
- **浏览页定案落地**:多选文件批量下载(1 批次→1 张父卡片,子项差分计量);本地排序(名称/大小/时间+升降,目录优先);删刷新按钮×3(设备页 1+浏览页 2)

### 配对修复
- **真实配对"死锁"根因修复**:PairingDialog 从设备页提升为 App.vue 全局挂载——此前离开设备页配对事件无人监听,同意门永不出现(新用户配对必失败);真机验证:任意页面+全自动配对闭环

### 已知限制
- 中继服务器地址暂不支持域名(validate 只收 IP:端口),公网部署填静态公网 IP;域名支持随 v0.13.x
- speedBps 显示口径偏乐观(QUIC 突发+EMA)；性能基线以秒表口径为准
- Android 手动添加暂不支持粘名片(仅 PC 端)
- 手机侧 Schtasks 会话显示器枚举偶发为空(PC 端截图降级为证据不作断言)
- 中继吞吐基线:WiFi 直连口径已有;公网中继路径已通过 relay-public 跨网验收(5MB 字节精确核对),吞吐量化待补测

## [0.12.0] - 2026-08-30

> **⚠️ 兼容性**:transfers.json 结构升级,旧版本读不回——v0.12.0 升级不可回退(降级前请先完成/清除活动任务)。

### 传输域重构(Task 1-12)
- **单卡片状态机**:传输卡收敛为 TransferCard 单写者入口(card_apply),非法迁移丢弃、终态吸收——杜绝幽灵行与状态复活
- **两级删除 + 历史记录**:活动删除先 cancelling(取消中)再实删;清除已完成 → 磁盘 manifest 历史入口,可恢复视图或彻底删除(parts 目录随之清除)
- **并发闸门**:全局并发任务数上限(默认 3,设置页可调 1-8),同对端仍串行,排队任务显示位次
- **父子卡片**:文件夹批量推送/拉取聚合为一张父卡,展开可见子文件进度,整批取消有效
- **双进度**:发送方显示对端确认进度(remote_done)与积压/等待确认提示
- **别名**:对端显示本地别名(空回退设备名)
- **共享区分区**:下载/共享目录默认初始化,列表超长别名/路径不撑破卡片
- **删手动刷新**:传输/设备页事件驱动自动刷新
- **GC 收窄**:parts 目录仅终态后清理,恢复链路不再误删
- **完成 toast 全覆盖**:拉取(下载完成)/推送(推送完成)仅活动→终态边触发,全局 toast 体系(切页仍可见)

## [0.11.0] - 2026-08-27

> **⚠️ 兼容性(重要)**:本版本为协议硬切换,v0.11.0 与旧版本不保证互通。**PC / Android / relay 三端须同步升级**(中继服务器上传新二进制后 restart,配置不动)。

### 安全(重点)
- **信任链三连修复**:中继/局域网握手指纹钉扎(防中间人)、注册私钥签名(防指纹抢注)、会话成员校验(防窃听)
- relay 服务防御加固:持锁推送超时、帧上限、认证超时、连接上限、端口 TTL、Punch 限速
- 反重放加固;拉取路径穿越修复
- 数据泄露专项:allowBackup 关闭、logcat 脱敏、CSP 收紧、PSK 掩码、剪贴板自清、秒传 oracle 消除、错误路径脱敏

### 稳定性
- BufferPool RAII 化(断传不再永久瘫痪)、响应通道失败归还
- 路由器重 IO 下放(大文件不再卡死控制面)
- FFI 连接超时与锁外执行(ANR 缓解)
- 会话代次(重连竞态)、配置读失败不回写、Android 主线程 IO 清理

### 架构
- RPC 按 msg_id 多路化(浏览与传输不再互斥)
- FFI OnceLock 无锁读、秒传位图化、save_dir 目录门禁

### 统计
- 全仓库审计 76 项全量修复

## [0.10.3] - 2026-08-26

### 新增
- **Android 本机 IP 复制与手动添加设备**:设备页新增本机信息卡(设备名+IP,一键复制发给对方);「+」打开手动添加弹窗(输 IP 或 IP:端口 → 探测 → 设备入列),探测结果含未发现原因提示(不在线/隐身);蜂窝网非私网 IP 附"可能无法直连"提示
- FFI 新增 `local_ip`/`probe_addr`(仅 Android+FFI 变更,PC/relay 零改动;协议无变更,relay 服务器无需更新)

### 修复
- **uniFFI 绑定重生成在 Windows 静默失败**:genUniffi 任务相对 `--out-dir` 不写文件但 exit 0,绑定从未更新;改为生成到绝对路径再拷回

## [0.10.2] - 2026-08-25

### 修复
- **隐身模式真正隐身**:中继 Register 上报隐身标志、服务端名册剔除隐身设备(半隐身:自己可见名册、可主动连接,别人发现不了你);隐身时不再发出主动探测(含周期重探);隐身开关翻转即时重注册中继(PC+Android)
- **Android ANR 卡死**:Repo 接口 suspend 化,FFI 调用(内部多锁+block_on)全部移出主线程
- **配对输码弹窗停留**:B 侧码匹配先回 PairResult 再本地收尾(输码方不再错过解除信号);Android 输码弹窗超时兜底(复用同意超时配置)+提交/连接错误不再静默
- **PC 配对成功卡片滞后**:配对成功事件即时重推设备列表(SessionUp 迟到不再卡旧状态)
- **PC UI**:设备卡片三点菜单加宽(230→280px,select 110→130px);手动添加设备弹窗输入框 border-box 防溢出

### 新增
- Android 本机文件长按菜单"发送到设备"(四 Tab 同构,单文件三步并一步)

## [0.10.1] - 2026-08-25

### 优化
- 文件页四 Tab 统一操作矩阵:相册/视频/文档补长按菜单(重命名/删除/选择多项)与多选删除;全部 Tab 单击文件直接进入多选;四 Tab 多选底部栏统一(发送+删除+单选重命名)
- 删除/重命名后按 Tab 刷新列表(MediaStore 重查/文档重扫/目录重列)

---
## [0.10.0] - 2026-08-25

### 新增
- **文件长按菜单**:本机/远程文件长按弹底部菜单(重命名/删除/选择多项;远程页含"下载到本机"),多选模式保留
- **远程单击直拉**:远程页单击文件(非目录)直接发起下载,Snackbar 可跳传输页——入口从三步缩为一步
- **大文件推送双进度条**:任务卡片内两条进度("已发送"+"对方已收"),RecvProgress 逐窗确认(16MB/条无洪泛);旧版对端自动降级隐藏第二条
- **收件箱 hash 秒传**:重复推送相同文件零字节传输——接收方收件箱索引命中(hash+size 双校验)本地复用(硬链优先);传输页显示"秒传"徽标;新旧对端互通(OfferFile.hash/MetaResp.file_hash/OfferResp.skip 全 serde default)

---

## [0.9.2] - 2026-08-24

### 修复
- 中继数据面 NAT 漂移跟踪:DATA 包源地址变化自动重学习,手机切网/运营商重分配后传输不再突然 0 字节
- 中继内层端点 keep-alive 对称:client 侧补挂 TransportConfig,双侧 idle 收紧 15s,死连接 15s 判死(原 30~60s)

### 新增
- 壳层自动重连:中继会话断开自动重 punch(退避 1s/2s,3 败停),设备卡片显示"重连中…"
- 壳层自动续传:对端恢复在线后自动续传 failed 且有断点的任务(每任务仅 1 次)

---

## v0.9.0 — 安卓中继支持 - 2026-08-24

### 新增
- **安卓完整中继支持**:启动自动重连、设备列表合并(本地+名册,指纹去重本地优先)、连接双路由(本地优先/名册兜底)、被动方向 PunchIncoming 接听——手机在 4G/异网可发现并连接家里/办公室 PC
- 设置页中继状态行:未启用/连接中/已连接/配置错误(带具体原因)
- save_settings 中继感知:配置或设备名变更自动重启中继连接(名册名字即时刷新)
- FFI `relay_status()` 拉模式查询接口,设置页无需事件订阅

### 修复
- 安卓 `devices()` 此前只读本地发现表,`via_relay` 恒 false——远程设备永不可见
- 安卓 `connect_device` 只走本地,名册设备报「Device not found」
- 此前手机保存中继配置后**不会自动连接**,配置生效须重启 App

## v0.8.3 — 设备体验修复(名册实名/列表稳定/中继配置校验) - 2026-08-24

### 修复
- **中结名册里所有设备都显示「LocalTrans」**:Register 上报名字此前硬编码,现上报真实设备名;本机改名后自动重连中继,名册秒级刷新
- **设备列表顺序随机跳动难以选中**:合并输出改为全序稳定排序(已连接 > 在线 > 名字 > 指纹),集合不变时卡片位置不动
- merge_devices/alias_map 下沉到 core,桌面与安卓共用一份实现(行为不变)

### 新增
- **中继配置保存校验(双端)**:缺端口/多冒号/密钥过短等错误保存时立即提示具体原因;坏配置重启后设置页显示「配置错误」而非永远「连接中」
- relay_status 返回 error 字段;FFI 新增 validate_relay(安卓侧规则同源)

### 说明
- 协议零变更(Register 本就携带 name 字段),**单端升级即生效**;名册实名需对端升级后重连一次

## v0.8.2 — 安全加固(P0 五项) - 2026-08-24

本版为安全修复版,协议无变更,旧版本对端不受影响;**接收侧升级即获保护**。

### 修复
- **推送文件名路径穿越(Critical)**:小文件批流与大文件 MetaResp 两条落盘链路统一净化(`sanitize_file_name`),拒绝绝对路径/`..`穿越/NTFS ADS/Windows 保留名/尾点尾空格,剔除 Unicode 控制与 bidi 伪装字符;`PartWriter::finalize` 设最终防线
- **批流声明 size 无上限导致接收进程 OOM 崩溃**:上限 1MiB,先校验后分配
- **大文件 total_size 与 Offer 声明不一致时拒绝接收**:防 part.bin 恶意高位扩展
- **移除信任后踢人失灵(Critical)**:此前移除信任不断开既有会话,且权限查询回落默认值反而全开;现移除即断开 + 全部权限查询 fail-closed(查不到=拒绝)
- **中继服务端配置缺失时静默回退 dev-psk 监听公网**:改为拒绝启动;PSK 最短 16 字符;启动日志输出 PSK 摘要供核对
- 子目录同名冲突文件被改名后落回根目录的问题

### 新增
- **远程删除共享区文件需本机确认**:双端弹窗(桌面模态/安卓 AlertDialog),60 秒超时自动拒绝,待确认并发上限 8,fail-closed
- 远程重命名/删除/新建文件夹需浏览权限(降权设备不再能操作);删除执行升级为 info 审计日志(指纹缩写,不含文件名)

## v0.8.1 — 弹窗中文化 + 视觉统一 + 逻辑件测试收尾 - 2026-08-23

### 新增
- **弹窗/文案全量中文化**:接收确认弹窗(收到传输请求/来自/文件数/倒计时/接收·拒绝)、传输失败原因中文映射(已手动移除/对方拒绝/等待超时等)、无障碍描述(新建文件夹/返回上级等 7 处)
- **逻辑件测试**:FailReasons(失败原因映射+重试判定,12 用例)与 TimeoutValidation(超时校验,6 用例)抽取为独立文件并补齐单测
- 设置页保存成功/失败 snackbar 反馈;超时输入越界显示具体提示文字;隐身模式附说明文字

### 修复
- 手动移除(removed)的传输记录不再显示"重试"按钮
- 超时输入非数字时此前不标红,现在统一标红

### 视觉
- 三处空态统一(56dp 图标/titleMedium 主标题/副文案去半透明);远程设备选择器标题改「选择远程设备」
- 设置页范围提示弱化(labelSmall);设备页指纹卡收紧;面包屑底部间距

### 说明
- 全部为安卓 UI 层改动,协议/FFI 零变更,单端升级即可
- dist 便携包:桌面(exe+relay+ubuntu tar+usage)+ 安卓(APK v0.8.1 versionCode 6 + usage,已去除相册备份章节)

## v0.8.0 — 安卓交互升级:传输管理 + 远程共享浏览 + 移除备份 - 2026-08-23

### 新增
- **传输页管理能力**:
  - 顶栏"清空记录"一键移除所有已完成/失败/已中断条目(进行中不受影响,二次确认)
  - 行级操作按状态分化:进行中 → 暂停/继续/取消;终态 → 删除(单条移除记录)
  - FFI 新增 `transfer_remove`(终态直删、活动任务先取消再删)与 `transfers_clear_finished`(批量清终态)两个 API
- **远程文件页真正可用**:切到"远程"Tab 先选已连接设备 → 浏览对方的共享文件夹(PC 端共享目录直接可见);多共享时 FilterChip 切换;空共享/无设备有明确引导
- **远程拉取闭环**:远程列表多选后底部操作栏可"下载"(pull),拉回本机 Download/LocalTrans
- **设备页直达共享**:已连接设备卡片新增"共享文件夹"按钮,一键跳到文件页并自动选中该设备的远程浏览

### 修复
- **传输状态徽章/按钮从未正确显示(存量 bug)**:UI 判断 `running` 而 FFI 实际发 `active`,暂停/继续按钮从未出现过;状态串全套对齐(active/pending/paused/done/failed/interrupted)并汉化
- 传输页空列表增加引导占位(此前白屏)
- pending 状态进度条改不确定式动画(避免 0% 静止误导)

### 移除
- **相册备份功能整体下线**:设置页备份卡片、BackupCoordinator 前台任务、MediaScanner 扫描链全部移除;SettingsDto 备份字段保留但保存时固定写关闭值(老配置自动失效,协议零变更)

### 说明
- FFI 层新增 2 个 API + UI 改动,core 协议零变更,**单端升级即可**
- uniffi 绑定重生成后 `AppException.message` 冲突手工修复(字段改名 errorMessage)——后续重生成绑定需重做此修复

## v0.7.1 — 修复:推送进度全链路 + 安卓改名不广播 - 2026-08-23

### 修复
- **手机推送进度全程 0%(根因×2)**:
  - FFI 推送事件泵只认 `Source*` 系事件,而 core 推送小文件走的是接收方风格 `Started/ChunkDone/Done/Failed`——全部落进 `_ => {}` 丢弃,占位任务永远 pending。已对齐桌面壳完整匹配(含 Done/Failed 占位 fallback)
  - FFI `SourceStarted` 用 get_mut 查表,但 source job_id 是 MetaReq 到达时新分配的、表中必无此行——行永远不建,后续 Source* 事件全部丢失。改为无条件插行
- **进度节流丢字节**:被 500ms 节流拦下的 ChunkDone/Speed 连传输表都不写,字节数凭空丢失、进度累计偏慢。改为"表始终写,节流只拦 UI 事件发射"(8 处,对齐桌面壳语义)
- **安卓改设备名后对端看到旧名/新旧并存**:FFI `save_settings` 改名后从不发 `DiscoveryCmd::SetName`,发现层出站名停留在旧值,配合 15s 过期表现为设备重复/时隐时现。已补改名检测+即时广播(对齐桌面壳)

### 说明
- 全部改动在 FFI 层(`crates/localtrans-ffi`),core 协议零变更,**单端升级即可**
- 新增 2 个 handler 级回归测试(Started 建行链路 / SourceStarted 无条件插行)

## v0.7.0 — 安卓落盘可见化 + 移动端传输交互优化 - 2026-08-23

### 新增
- **接收落盘用户可见**:手机收到的文件落到 `Download/LocalTrans/`(图片/视频经 MediaScanner 自动进系统相册;文档文件管理器天然可见)。断点续传内部状态仍在私有目录不污染用户空间
- **文件页四 Tab**:本机页新增 [相册] [视频] [文档] [全部] 二级 Tab——相册/视频为 MediaStore 网格缩略图(Coil,视频时长角标),文档 Tab 扫描 Download/Documents/DCIM 常见文档类型,"全部"保留原目录浏览
- **SendBar 发送**:任意 Tab 多选后底部发送栏(已选数+大小汇总),设备底弹选择即发;**"全部"Tab 支持选文件夹推送,远端目录结构保持**
- **传输页增强**:实时速度+剩余时间、文件类型图标、完成行缩略图、点击完成行经 FileProvider 唤起系统查看器打开、通知点击直达传输页

### 修复
- FFI `FilesSaved` 事件精确通知 Kotlin 落盘路径(小文件批流每文件 Done 快照累积,跨文件路径列表完整)
- MediaStore 排序串兼容小写列名设备 + 查询异常兜底(不崩溃)
- inbox 注入时机(须在 FFI start() 之后)
- gitignore `data/` 规则误伤安卓 data 包(四个 Repo 文件此前未入库,克隆即缺)

### 注意
- 协议零变更,**单端升级即可**(桌面 v0.5.0+ 与本版互通)
- 完整双机人工验证清单见 docs/e2e-android-manual.md

## v0.6.1 — 传输终态完整化 + 设备名体系 - 2026-08-23

### 修复
- **传输结束后数据源方计时不停(根因修复)**:RecvAck 双向确认补齐两处漏发——推送大文件接收后、空文件拉取后均回执,发送方 source 行正确落"已完成"并冻结计时(v0.2.4 契约只覆盖了普通下载路径)
- **推送大文件偶发 30 秒超时失败(存量生产 bug)**:大文件注册 register_push_job_rel 原在拿到 OfferResp 之后,与接收方 accept 后立即反向发的 MetaReq 赛跑——输掉时 MetaReq 被丢弃、接收方等 MetaResp 30s 超时。注册提前到发 OfferReq 之前
- 取消/失败的传输在数据源侧不再永久"进行中":复用 TransferCtl{Cancel} 通知数据源清理 sender 任务(下载取消场景);推送失败按 offer_id 关联清理发送方 source 行(磁盘满/对端拒收场景)。连接断开仍有 30s 兜底
- 终态后进度聚合泵从 4Hz 降回空闲 1Hz、发送方探针即时停止,常驻 CPU 进一步下降

### 新增
- 设备名体系:设置页改设备名即时生效(连发一轮 Presence,对方秒级看到,无需重启);信任设备支持本地备注(别名优先于对方广播名,仅本机视角);对方改名本机自动跟随
- 安卓首次安装默认设备名"我的手机"(此前沿用桌面默认"我的电脑",造成"本机被重复识别"观感)
- 重探目标失联淘汰:连续 ~80s 无回应的地址自动移除(模拟器换 IP 后的僵尸地址不再永久残留)
- PC 设备卡片下拉菜单溢出修复(贴近视口底部改为内部滚动,权限/推送行布局收紧)

### 注意
- 本版本传输确认协议变更,**两端都需升级**才生效(桌面重下/手机重装)
- 需手动验证:两设备互传大文件中途取消 → 对方传输页应在 1 秒内落"失败(对方已取消)"且计时冻结

## v0.6.0 — 安卓客户端首发 - 2026-08-23

### 新增
- Android 客户端(Kotlin + Jetpack Compose + uniFFI 复用 Rust core):发现/配对(同意门+配对码)/推拉互传/接收确认(倒计时 BottomSheet)与桌面 v0.5.0 互通
- 移动端文件浏览器:本地+远程浏览、重命名/删除/新建、搜索过滤、系统"打开方式"、SAF 文件选择
- 相册自动备份(前台增量,MediaStore 游标持久化,存对方共享区 LocalTransBackup/<设备名>/)
- core:share 协议新增 ShareRename/ShareDelete/ShareMkdir/ShareOpResult(远程文件操作;老版本收新消息按未知丢弃不崩,老对端操作返回"不支持"错误)
- crates/localtrans-ffi:uniFFI 0.28 facade——AppEvent 13 事件+全量 API,ffi_guard panic 防护,进度 500ms 节流,与桌面壳同构的 xfer_lock/占位任务/看门狗
- 设置页:设备名/隐身/中继/双超时(15-600 钳制)/备份目标;传输完成系统通知

### 已知限制
- 桌面端 v0.5.0 与安卓互传可用;远程文件操作需两端 v0.6.0+
- 相册备份为前台模式(杀 App 即停,重开续传);完整 E2E 双机人工验证清单见 docs/e2e-android-manual.md

## v0.5.0 — 推送体验完善(双入口 + 确认倒计时 + 拒绝原因) - 2026-08-22

### 新增
- 推送双入口：设备卡片【推送文件…】+ 设备页推送向导（选设备→选文件→发送）；拖放保留
- 接收确认弹窗升级：可配置倒计时（默认 60s，15-600 可调）、总大小汇总、长列表折叠、"另存到…"自动顺延
- 拒绝原因传播：对方拒绝/超时在发送方任务上明确标出，拒绝/超时任务支持一键重发
- 自动接受档接收完成后系统通知（不无声被塞文件）

### 修复
- relay data_plane 测试并行共享端口池在 Windows SO_REUSEADDR 下互踩（预存 flaky，#[serial] 修复）

## v0.4.0 — 配对授权强化(同意门 + 单向随机码) - 2026-08-22

### 新增
- 同意门:被连接方需先点击"同意连接",配对码才生成并展示(默认 60s 超时,可在设置页调整 15-600s)
- 配对码一次一随机:每次配对尝试独立生成,连接结束即焚;只在被连接方屏幕显示,发起方界面永不显示
- 结束等待:被连接方同意后可随时终止配对等待(码即时作废)
- 设置页新增"连接安全"卡片(同意超时配置)

### 安全
- 配对完成必要条件 = 被连接方同意 + 发起方输对码;核心态哈希常数时间比对,明文码不落盘、不进日志与协议
- B 侧主动结束等待 / 拒绝 / 门超时均断连且不记冷却;码错 3 次断连+冷却 5min(保留)

### 修复
- 中继远程配对从未真正走通的历史 bug:发起方对内层连接误用被动方语义(双方互相 accept_bi 死锁 10s),现发起方走 adopt_as_initiator;中继内层连接补充自签客户端证书(对端此前 NoPeerCert)
- 控制流爆发测试在慢调度环境(WSL 单核)的时序脆弱:断言改为帧序不变式+静默期结束

## v0.2.0 — 传输面板源端可视化 + 持久化 + 卡死修复

### 新增
- 发送方(推送方 / 服务方)在 Transfers 页可见
- 角色徽章：接收方 / 推送方 / 被取方
- 健康面板：丢包率 / RTT / 拥塞窗口 / 并发流数
- ETA 倒计时
- 三件套：暂停 / 继续 / 取消(全角色)
- 服务方控制：限速(1/2/4 流)/ 踢人
- transfers.json 持久化(节流 1s 写盘)
- 启动恢复：active → interrupted 迁移
- 已中断(interrupted)状态与续传横幅
- 关闭应用时弹模态提醒

### 修复
- u64 job_id 跨 JSON 边界精度丢失
- 全小文件 push 卡"等待中"
- Done/Failed 找不到 job_id 时占位 fallback

---

## v0.1.0 — 初始版本
- 基础传输功能
- 配对与发现机制
- 文件拉取与推送
