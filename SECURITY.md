# 安全策略

## 报告漏洞

请通过代码托管平台的**私密安全报告渠道**提交（GitHub Security Advisory /
GitLab 机密 Issue），不要在公开 Issue 中描述可被利用的细节。通常 72 小时内回应。

## 安全模型速览

- **身份**：每台设备一把 Ed25519 密钥，指纹（证书 DER SHA-256）是唯一身份
  标识；配对采用带外 6 位码（只在被连接方屏幕显示，从不在网络传输），
  常量时间比较、3 次错码冷却 300 秒。
- **传输**：QUIC + TLS 1.3 双向 mTLS，每次连接做证书指纹钉扎——配对后的
  中间人无法插入。
- **权限**：浏览/下载/推送三类权限按指纹逐台配置，在**接收侧**强制执行，
  未配对默认全拒；远端提供的文件名与目录分量全部经过路径穿越净化。
- **中继**（可选自部署）：纯 UDP 盲转发，内层为端到端 mTLS 会话，服务端
  看不到文件名/大小/内容；名册与租约仅存内存。
- 完整设计与取舍见 [DESIGN.md](DESIGN.md)。

## 已知取舍（透明披露）

- 局域网发现广播为**明文**（带签名防伪造/防重放）：同一局域网的观察者可以
  被动得知设备名、指纹与 IP。这是发现协议的性能/兼容性取舍，不泄露传输内容。
- 身份私钥 `data/identity.key` 与中继 PSK（若配置）在本机**明文存储**，
  依赖操作系统目录权限保护；未接入 DPAPI/Keystore（打磨项）。
- 中继地址当前仅支持 `IP:端口` 字面量，不支持域名（校验器限制，打磨项）。
