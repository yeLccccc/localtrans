# 性能基线报告（P4）

> 由 e2e perf 场景自动再生（tests/e2e/lib/perfreport.mjs）——手工勿改，复跑即刷新。
> 三个段落各自标注最近一次绿跑日期；场景失败不覆盖旧数据。

- **口径**：PC→手机 **WiFi 实测**（真实用户主路径；手机 USB 仅作 adb 控制面，传输走 QUIC/WiFi）。
  huss_laptop PC-PC LAN 为可选对照段；中继口径留双机遗留（条件跳过，不部署中继）。
- **日期**：2026-09-09 00:27
- **环境**：huss_pc（release 测试构建，test-api）+ huss_phone（M2002J9E，MIUI 12）。
- **单位**：1 MB = 1 MiB = 1048576 字节；CPU% 为单核口径（100% = 跑满 1 核，Win32 PerfProc PercentProcessorTime）。
- **证据说明**：测试时段开发机屏幕锁定时，窗口截图（GDI 桌面裁剪）拍到的是锁屏壁纸——
  渲染正确性以 DOM 断言（uiTree/uiWait）为准，截图仅作过程证据（shot.rs 设计原则）。

## T1 大文件吞吐 + T2 小文件批量（perf-baseline，2026-09-08 18:25）

| 轮次 | 文件 | 字节 | 耗时 | 全程 MB/s | 发送端 CPU 均值（单核%） | 结束内存 MB |
|---|---|---|---|---|---|---|
| 1 | perf1g-w185-r1.bin | 1073741824 | 387.7s | 2.6 | 14.6 | 58.0 |
| 2 | perf1g-w185-r2.bin | 1073741824 | 390.7s | 2.6 | 2.5 | 59.2 |
| 3 | perf1g-w185-r3.bin | 1073741824 | 387.9s | 2.6 | 2.4 | 63.9 |
| **中位** | — | 1073741824 | 387.9s | **2.6** | 2.4 | 63.9 |

**T2 小文件批量（200×10KB 单 offer）**：耗时 19.93s，吞吐 0.1 MB/s（2048000 字节，父卡 done）

对照与观测：
- huss_laptop 环回对照：1×500MB 3.3 MB/s（PC-PC LAN，非主口径）
- 中继口径：relay_status={"connected":false,"devices":0,"enabled":false,"error":"","server":""} → 中继吞吐口径留双机遗留（中继未启用）
- T1-r1: 发送端 speedBps 显示口径中位 64.5 MB/s,秒表端到端 2.6 MB/s（发送端 done 按 ~8MB QUIC 突发推进,EMA 显示口径偏乐观——基线以秒表口径为准,显示口径偏差留打磨池核查）
- T1-r2: 发送端 speedBps 显示口径中位 64.4 MB/s,秒表端到端 2.6 MB/s（发送端 done 按 ~8MB QUIC 突发推进,EMA 显示口径偏乐观——基线以秒表口径为准,显示口径偏差留打磨池核查）
- T1-r3: 发送端 speedBps 显示口径中位 64.5 MB/s,秒表端到端 2.6 MB/s（发送端 done 按 ~8MB QUIC 突发推进,EMA 显示口径偏乐观——基线以秒表口径为准,显示口径偏差留打磨池核查）

## T3 前端渲染 1000 卡（perf-render，2026-09-09 00:22）

| 指标 | 值 | 断言 |
|---|---|---|
| 历史卡数 | 1000 | ≥1000 |
| 1000 卡展开首屏（点击→末卡 uiWait 命中） | 111 ms | <2000 ms ✅ |
| 页面导航首屏（navigate→折叠条可见） | 21 ms | 记录值 |
| 交互 DOM 节点数（uiTree 可见交互元素口径） | 2007 | ≥1000 |

滚动流畅度：人工目检项（webview 无程序化滚动注入面），证据截图 reports/perf-render-2026-09-09-00-22-02-nh6o 目检。

## T4 空闲足迹 5 分钟（perf-idle，2026-09-09 00:27）

| 指标 | 值 | 断言 |
|---|---|---|
| CPU 均值（单核口径） | 0.00% | <2% ✅ |
| CPU 峰值 | 0.0% | >5% 单点 0 个（无异常） |
| 内存（起→末） | 31.7 → 33.7 MB | 记录值 |

采样序列（每 30s）：0s:0.0% 30s:0.0% 60s:0.0% 90s:0.0% 120s:0.0% 150s:0.0% 180s:0.0% 210s:0.0% 240s:0.0% 270s:0.0%

备注：无 >5% 单点,未见周期扫描异常

## 复跑方式

```bash
cd tests/e2e && node lib/deploy.mjs --skip-huss_laptop   # PC 测试构建
node scenarios/perf-baseline.mjs   # T1+T2（需手机在线；不可达自动条件跳过）
node scenarios/perf-render.mjs     # T3（PC 单机）
node scenarios/perf-idle.mjs       # T4（PC 单机，约 6 分钟）
```

回归判定：与本基线比，吞吐 ±20% 标红（人工/评审口径；场景断言只锁完成性与上限，
不锁具体速率，避免 WiFi 波动假失败）。
