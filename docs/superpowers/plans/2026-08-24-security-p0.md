# 安全加固 P0 五项 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地 spec `docs/superpowers/specs/2026-08-24-security-hardening-design.md` 的 P0 五项:推送落盘文件名净化、远程文件操作权限门+删除确认、relay 严格启动、批流 size 上限、移除信任即时生效。

**Architecture:** 全部改动在接收侧/被操作侧/服务端,协议(ControlMsg 结构)零变更——DeleteAsk 是本机进程内事件,不走网络。core(crates/localtrans-core)承担全部防线;两个壳层(src-tauri 桌面 / localtrans-ffi 安卓)只接线删除确认的 UI 事件与应答。

**Tech Stack:** Rust(tokio/quinn)、Tauri v2 + Vue 3、uniFFI + Kotlin Compose。

## Global Constraints

- 提交信息:中文前缀(fix:/feat:/test:/chore:/docs:)+ 空行 + `Co-Authored-By: Claude <noreply@anthropic.com>`
- **明文文件名/路径不进 tracing 日志**:日志只打 job_id/ask_id/share_id/数量/指纹 hex 缩写(`hex::encode(&fp[..4])`)
- **fail-closed 原则**:信任表查不到对端 = 一律拒绝,禁止 `Perms::default()`(browse/download 全开)作为回退
- 协议零变更:不修改 `crates/localtrans-core/src/protocol.rs` 的任何 ControlMsg 定义
- 测试命令:`cargo test -p localtrans-core` / `cargo test -p localtrans-relay`;构建检查 `cargo check -p localtrans-ffi`
- 本计划引用的 engine.rs 行号是 v0.8.1 基线(88703d9 之前的 e7a6f35),实现时**以内容定位为准**,行号会有 ±30 漂移
- 测试风格:engine.rs 内联 `#[cfg(test)] mod tests`(约 2600 行起),follow 既有 harness 模式;不新建集成测试文件
- **与 spec 的偏差(已定案)**:spec 的"2 个集成测试"改为**提取可测缝的单元测试**(write_small_file / PartWriter::finalize 直测)——恶意文件名无法在真实文件系统上作为发送方文件存在,全网络 e2e 无法覆盖接收侧防线,单元测试等价覆盖且稳定

---

### Task 1: sanitize_file_name 纯函数(T1 红绿灯)

**Files:**
- Modify: `crates/localtrans-core/src/transfer/mod.rs`(函数 + 内联测试)

**Interfaces:**
- Produces: `pub fn sanitize_file_name(name: &str) -> Result<String, EngineError>`(Task 2/3 消费;EngineError 已从 engine.rs re-export 在 mod.rs)

- [ ] **Step 1: 写失败测试**

在 `crates/localtrans-core/src/transfer/mod.rs` 末尾追加:

```rust
#[cfg(test)]
mod sanitize_file_name_tests {
    use super::sanitize_file_name;

    #[test]
    fn normal_names_pass_untouched() {
        assert_eq!(sanitize_file_name("报告.pdf").unwrap(), "报告.pdf");
        assert_eq!(sanitize_file_name("movie (2026) [1080p].mkv").unwrap(), "movie (2026) [1080p].mkv");
        assert_eq!(sanitize_file_name("照片🎉.png").unwrap(), "照片🎉.png");
        assert_eq!(sanitize_file_name("a.b.c.txt").unwrap(), "a.b.c.txt");
    }

    #[test]
    fn empty_rejected() {
        assert!(sanitize_file_name("").is_err());
    }

    #[test]
    fn separators_rejected() {
        for bad in ["a/b", "a\\b", "C:\\Users\\x\\evil.bat", "\\\\?\\C:\\x", "a:b", "a*b", "a?b", "a\"b", "a<b", "a>b", "a|b", "..\\..\\evil.dll"] {
            assert!(sanitize_file_name(bad).is_err(), "应拒绝: {:?}", bad);
        }
    }

    #[test]
    fn dot_names_rejected() {
        assert!(sanitize_file_name(".").is_err());
        assert!(sanitize_file_name("..").is_err());
    }

    #[test]
    fn windows_reserved_rejected() {
        for bad in ["CON", "con", "NUL.txt", "com1", "LPT1.log", "PRN", "aux", "COM9.tar.gz"] {
            assert!(sanitize_file_name(bad).is_err(), "应拒绝保留名: {:?}", bad);
        }
    }

    #[test]
    fn trailing_dot_or_space_rejected() {
        assert!(sanitize_file_name("evil.exe.").is_err());
        assert!(sanitize_file_name("evil.exe ").is_err());
    }

    #[test]
    fn too_long_rejected() {
        let long = "a".repeat(256);
        assert!(sanitize_file_name(&long).is_err());
        let ok = "a".repeat(255);
        assert!(sanitize_file_name(&ok).is_ok());
    }

    #[test]
    fn control_chars_stripped_not_rejected() {
        assert_eq!(sanitize_file_name("a\u{0000}b.txt").unwrap(), "ab.txt");
        assert_eq!(sanitize_file_name("a\u{007F}b").unwrap(), "ab");
        // 剔除后为空 → 拒绝
        assert!(sanitize_file_name("\u{0001}").is_err());
    }

    #[test]
    fn bidi_controls_stripped() {
        // U+202E RTL override 伪装扩展名:剔除后是正常名,不拒绝
        assert_eq!(sanitize_file_name("fdp\u{202E}exe.pdf").unwrap(), "fdpexe.pdf");
        assert_eq!(sanitize_file_name("a\u{2066}b.txt").unwrap(), "ab.txt");
    }

    #[test]
    fn error_message_contains_no_original_name() {
        let err = sanitize_file_name("..\\..\\secret\\evil.dll").unwrap_err();
        let msg = format!("{:?}", err);
        assert!(!msg.contains("secret"), "错误信息不得回显原始文件名: {}", msg);
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p localtrans-core sanitize_file_name`
Expected: 编译失败 `cannot find function sanitize_file_name`

- [ ] **Step 3: 实现**

在 `crates/localtrans-core/src/transfer/mod.rs` 的 `pending_jobs` 函数之前插入:

```rust
/// P0-1: 推送落盘文件名净化(拒绝制为主、剔除为辅)。
/// 拒绝:空串 / 含分隔符与非法字符 / `.`/`..` / Windows 保留名 / 尾点尾空格 / 净化后超 255 字节。
/// 剔除:Unicode 控制字符与 bidi 控制符(剔除后为空则拒绝)。
/// 错误信息只含类别,不回显原始名(明文名不进日志规约)。
pub fn sanitize_file_name(name: &str) -> Result<String, EngineError> {
    const RESERVED: [&str; 22] = [
        "CON", "PRN", "AUX", "NUL",
        "COM1", "COM2", "COM3", "COM4", "COM5", "COM6", "COM7", "COM8", "COM9",
        "LPT1", "LPT2", "LPT3", "LPT4", "LPT5", "LPT6", "LPT7", "LPT8", "LPT9",
    ];

    // 1) 剔除控制字符与 bidi 控制符(改写,不拒绝)
    let mut cleaned = String::with_capacity(name.len());
    for c in name.chars() {
        let is_ctrl = matches!(c as u32, 0x0000..=0x001F | 0x007F | 0x202A..=0x202E | 0x2066..=0x2069);
        if !is_ctrl {
            cleaned.push(c);
        }
    }

    // 2) 拒绝制校验(顺序即错误类别)
    if cleaned.is_empty() {
        return Err(EngineError::Protocol("非法文件名(为空)".into()));
    }
    if cleaned.chars().any(|c| matches!(c, '/' | '\\' | ':' | '*' | '?' | '"' | '<' | '>' | '|')) {
        return Err(EngineError::Protocol("非法文件名(含分隔符或非法字符)".into()));
    }
    if cleaned == "." || cleaned == ".." {
        return Err(EngineError::Protocol("非法文件名(穿越目录)".into()));
    }
    let stem_upper = cleaned.split('.').next().unwrap_or("").to_ascii_uppercase();
    if RESERVED.contains(&stem_upper.as_str()) {
        return Err(EngineError::Protocol("非法文件名(Windows 保留名)".into()));
    }
    if cleaned.ends_with('.') || cleaned.ends_with(' ') {
        return Err(EngineError::Protocol("非法文件名(尾点或尾空格)".into()));
    }
    if cleaned.len() > 255 {
        return Err(EngineError::Protocol("非法文件名(超过 255 字节)".into()));
    }
    Ok(cleaned)
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test -p localtrans-core sanitize_file_name`
Expected: 9 passed

- [ ] **Step 5: 全量回归 + 提交**

Run: `cargo test -p localtrans-core`
Expected: 既有测试全绿
```bash
git add crates/localtrans-core/src/transfer/mod.rs
git commit -m "feat(security): sanitize_file_name 文件名净化纯函数(P0-1)

拒绝分隔符/穿越/保留名/尾点/超长,剔除控制与 bidi 字符;
错误信息不含原始名。

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 2: 小文件批流接入净化 + size 上限 + 冲突路径修复(P0-1a + P0-4)

**Files:**
- Modify: `crates/localtrans-core/src/transfer/engine.rs`(批流接收函数 `recv_small_files_batched` 内,基线行 2150-2225 一带;新增 `write_small_file` 辅助函数放在 `sanitize_component`(基线 2407)附近)

**Interfaces:**
- Consumes: Task 1 的 `crate::transfer::sanitize_file_name`;既有 `SMALL_FILE_LIMIT`(engine.rs 内 `pub const SMALL_FILE_LIMIT: u64 = 1024 * 1024;`)
- Produces: `fn write_small_file(dest_dir: &Path, file_name: &str, data: &[u8]) -> std::io::Result<PathBuf>`(同文件私有)

- [ ] **Step 1: 写失败测试**

在 engine.rs 文件末尾的 `#[cfg(test)] mod tests` 内追加(write_small_file 是私有函数,测试放同文件 tests mod):

```rust
#[test]
fn write_small_file_conflict_rename_in_same_dir() {
    let root = std::env::temp_dir().join(format!("lt-wsf-{}-conflict", std::process::id()));
    let sub = root.join("sub");
    std::fs::create_dir_all(&sub).unwrap();
    // 第一个文件落在子目录
    write_small_file(&sub, "a.txt", b"1").unwrap();
    // 同名冲突:改名文件必须仍在子目录(修复 v0.8.1 前误用 save_dir 落回根目录的 bug)
    let p2 = write_small_file(&sub, "a.txt", b"2").unwrap();
    assert!(p2.starts_with(&sub), "冲突改名不得跳出目标目录: {:?}", p2);
    assert_eq!(p2.file_name().unwrap().to_str().unwrap(), "a.txt (1)");
    // 已存在文件不被覆盖
    assert_eq!(std::fs::read(sub.join("a.txt")).unwrap(), b"1");
    let _ = std::fs::remove_dir_all(&root);
}

#[test]
fn write_small_file_plain_write() {
    let root = std::env::temp_dir().join(format!("lt-wsf-{}-plain", std::process::id()));
    std::fs::create_dir_all(&root).unwrap();
    let p = write_small_file(&root, "hello.txt", b"hi").unwrap();
    assert_eq!(p, root.join("hello.txt"));
    assert_eq!(std::fs::read(&p).unwrap(), b"hi");
    let _ = std::fs::remove_dir_all(&root);
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p localtrans-core write_small_file`
Expected: 编译失败 `cannot find function write_small_file`

- [ ] **Step 3: 实现 write_small_file 并改造批流**

3a. 在 `sanitize_component` 函数附近新增:

```rust
/// P0-1a/P0-4: 批流单文件落盘。调用前提:name 已过 sanitize_file_name、size 已校验。
/// 冲突改名基于 dest_dir(v0.8.1 前误用 save_dir,子目录冲突文件会落回根目录——已修)。
fn write_small_file(dest_dir: &Path, file_name: &str, data: &[u8]) -> std::io::Result<PathBuf> {
    let file_path = dest_dir.join(file_name);
    let final_path = if file_path.exists() {
        let mut counter = 1u32;
        loop {
            let new_name = format!("{} ({})", file_name, counter);
            let new_path = dest_dir.join(&new_name);
            if !new_path.exists() {
                break new_path;
            }
            counter += 1;
        }
    } else {
        file_path
    };
    std::fs::write(&final_path, data)?;
    Ok(final_path)
}
```

3b. 改造 `recv_small_files_batched` 内的接收循环(定位锚点:注释 `// 读取 [名 UTF-8]` 到 `fs::write(&final_path, data)?;`):

在读名之后(锚点 `let file_name = String::from_utf8(name_bytes)...?;` 之后)插入:

```rust
        // P0-1a: 文件名净化(拒绝穿越/绝对路径/ADS/保留名等)
        let file_name = crate::transfer::sanitize_file_name(&file_name)?;
```

在读 size 之后(锚点 `let file_size = u64::from_be_bytes(size_bytes);` 之后)插入:

```rust
        // P0-4: 声明大小上限校验——超限即断流拒绝,杜绝 vec![0u8; u64::MAX] 式 OOM
        if file_size > SMALL_FILE_LIMIT {
            return Err(EngineError::Protocol(format!(
                "批流文件超过小文件上限: 声明 {} 字节, 上限 {}",
                file_size, SMALL_FILE_LIMIT
            )));
        }
```

把落盘段(锚点从 `// 写入文件(v0.2.6:rel_dir 子目录...` 到 `fs::write(&final_path, data)?;` 的整段冲突处理 + 写入)替换为:

```rust
        // 写入文件(v0.2.6:rel_dir 子目录,成分逐个净化防穿越;P0-1a:文件名已净化)
        let mut dest_dir = save_dir.to_path_buf();
        for comp in rel_dir.split('/').filter(|s| !s.is_empty()) {
            dest_dir.push(sanitize_component(comp));
        }
        fs::create_dir_all(&dest_dir)?;
        write_small_file(&dest_dir, &file_name, &data)?;
```

- [ ] **Step 4: 跑测试确认通过 + 回归**

Run: `cargo test -p localtrans-core write_small_file && cargo test -p localtrans-core`
Expected: 新增 2 个 + 既有全绿

- [ ] **Step 5: 提交**

```bash
git add crates/localtrans-core/src/transfer/engine.rs
git commit -m "fix(security): 批流文件名净化接入 + size 上限 + 冲突路径修复(P0-1a/P0-4)

拒绝超 1MiB 声明防 OOM;冲突改名不再落回根目录。

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 3: MetaResp 入口净化 + total_size 交叉校验 + finalize 最终防线(P0-1b)

**Files:**
- Modify: `crates/localtrans-core/src/transfer/engine.rs`(`recv_push_large_file` 基线 1104-1160 一带;`PartWriter::finalize` 基线 250-262)

**Interfaces:**
- Consumes: Task 1 `sanitize_file_name`
- Produces: 无新接口(防线内嵌)

- [ ] **Step 1: 写失败测试(finalize 防线直测)**

engine.rs tests mod 追加:

```rust
#[test]
fn finalize_rejects_hostile_manifest_name() {
    let root = std::env::temp_dir().join(format!("lt-fin-{}-hostile", std::process::id()));
    let parts = root.join("parts/job1");
    let dest = root.join("dest");
    std::fs::create_dir_all(&parts).unwrap();
    std::fs::create_dir_all(&dest).unwrap();

    // 恶意清单:文件名带穿越(模拟 MetaResp 攻击字段)
    let manifest = Manifest::from_meta("..\\..\\evil.dll".to_string(), 8, vec![[0u8; 32]]);
    let writer = PartWriter::load_or_open(&parts, manifest).unwrap();
    let err = writer.finalize(&dest).err().expect("恶意名必须被 finalize 拒绝");
    assert!(matches!(err, EngineError::Protocol(_)), "实际: {:?}", err);
    // part 文件未被移动出 parts 目录
    assert!(!dest.join("evil.dll").exists());
    let _ = std::fs::remove_dir_all(&root);
}
```

(若 `Manifest::from_meta` 的 chunk_hashes 参数类型不是 `Vec<[u8; 32]>`,以实际签名为准改为等价构造——它是 MetaResp 解构出的字段类型,engine.rs 基线 1147 有调用示例。)

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p localtrans-core finalize_rejects`
Expected: FAIL——finalize 当前不校验,把 part 移到了 dest 外的穿越路径(或 err 为 None)

- [ ] **Step 3: 实现**

3a. `PartWriter::finalize` 方法体**第一行**(任何 sync/rename 之前)插入:

```rust
        // P0-1b 最终防线:清单文件名再净化(防未来调用点漏过入口校验)
        crate::transfer::sanitize_file_name(&self.manifest.file_name)?;
```

3b. `recv_push_large_file` 内 MetaResp 解构之后、构造 Manifest 之前(锚点:`let (job_id, file_name, total_size, chunk_hashes) = meta_wait...` 与 `let manifest = Manifest::from_meta(...)` 之间)插入:

```rust
    // P0-1b: MetaResp 文件名净化(攻击者全权可控字段)
    let file_name = crate::transfer::sanitize_file_name(&file_name)?;
    // P0-1b/P0-4: total_size 与 OfferReq 声明交叉校验(挡"offer 报小、meta 报大"的
    // part.bin 高位偏移扩展攻击),不允许零头差异
    if total_size != file.size {
        return Err(EngineError::Protocol(format!(
            "MetaResp total_size ({}) 与 Offer 声明 ({}) 不一致, 拒绝接收",
            total_size, file.size
        )));
    }
```

- [ ] **Step 4: 跑测试 + 回归**

Run: `cargo test -p localtrans-core finalize_rejects && cargo test -p localtrans-core`
Expected: 新增 1 个通过;若既有推送测试因 total_size 交叉校验失败,检查测试是否本来就让两者不一致(正常推送路径两者同源,应全绿)

- [ ] **Step 5: 提交**

```bash
git add crates/localtrans-core/src/transfer/engine.rs
git commit -m "fix(security): MetaResp 文件名净化 + total_size 交叉校验 + finalize 防线(P0-1b)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 4: relay 严格启动——缺配置/弱 PSK 拒启(P0-3)

**Files:**
- Modify: `crates/localtrans-relay/src/config.rs`(load_or_default → load,加 psk_digest;新增 tests mod)
- Modify: `crates/localtrans-relay/src/main.rs`(调用点 + 摘要日志)
- Modify: `crates/localtrans-relay/Cargo.toml`(加 sha2)

**Interfaces:**
- Produces: `pub fn load(path: &Path) -> Result<Self, String>`(替代 load_or_default);`pub fn psk_digest(psk: &str) -> String`(8 hex 字符)

- [ ] **Step 1: 写失败测试**

config.rs 末尾追加:

```rust
#[cfg(test)]
mod strict_load_tests {
    use super::*;

    fn write_cfg(dir: &std::path::Path, content: &str) -> std::path::PathBuf {
        let p = dir.join("relay.toml");
        std::fs::write(&p, content).unwrap();
        p
    }

    #[test]
    fn missing_file_rejected() {
        let err = RelayConfig::load(std::path::Path::new("Z:/不存在/relay.toml")).unwrap_err();
        assert!(err.contains("拒绝启动"), "实际: {}", err);
    }

    #[test]
    fn short_psk_rejected() {
        let tmp = std::env::temp_dir().join(format!("lt-relay-{}-short", std::process::id()));
        std::fs::create_dir_all(&tmp).unwrap();
        let p = write_cfg(&tmp, "control_port = 9443\npsk = \"short\"\n");
        let err = RelayConfig::load(&p).unwrap_err();
        assert!(err.contains("16"), "实际: {}", err);
        let _ = std::fs::remove_dir_all(&tmp);
    }

    #[test]
    fn valid_config_loads() {
        let tmp = std::env::temp_dir().join(format!("lt-relay-{}-ok", std::process::id()));
        std::fs::create_dir_all(&tmp).unwrap();
        let p = write_cfg(&tmp, "psk = \"0123456789abcdef0123456789abcdef\"\n");
        let cfg = RelayConfig::load(&p).unwrap();
        assert_eq!(cfg.psk, "0123456789abcdef0123456789abcdef");
        let _ = std::fs::remove_dir_all(&tmp);
    }

    #[test]
    fn psk_digest_is_8_hex() {
        let d = RelayConfig::psk_digest("whatever-psk-value");
        assert_eq!(d.len(), 8);
        assert!(d.chars().all(|c| c.is_ascii_hexdigit()), "实际: {}", d);
        // 确定性:同 PSK 同摘要
        assert_eq!(d, RelayConfig::psk_digest("whatever-psk-value"));
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p localtrans-relay strict_load`
Expected: 编译失败 `no function named load` / `psk_digest`

- [ ] **Step 3: 实现**

3a. `Cargo.toml` `[dependencies]` 加一行:

```toml
sha2 = "0.10"
```

3b. config.rs 把 `load_or_default` 整个函数替换为:

```rust
    /// P0-3: 严格加载——配置文件缺失/解析失败/psk 过短一律拒绝启动。
    /// (v0.8.1 前缺失时静默回退 dev-psk 并监听 0.0.0.0,公网裸奔)
    pub fn load(path: &Path) -> Result<Self, String> {
        let content = std::fs::read_to_string(path)
            .map_err(|_| format!("配置文件 {} 不存在或不可读, 拒绝启动。必须显式配置 psk。", path.display()))?;
        let cfg = Self::from_toml_str(&content)
            .map_err(|e| format!("配置文件 {} 解析失败 ({}) , 拒绝启动。", path.display(), e))?;
        if cfg.psk.len() < 16 {
            return Err(format!(
                "psk 长度不足 16 字符 (当前 {}), 拒绝启动。建议 32+ 随机字节 hex。", cfg.psk.len()
            ));
        }
        Ok(cfg)
    }

    /// PSK 摘要(sha256 前 8 hex)——启动日志供运维核对配置是否为预期值,不暴露 PSK
    pub fn psk_digest(psk: &str) -> String {
        use sha2::{Digest, Sha256};
        let mut h = Sha256::new();
        h.update(psk.as_bytes());
        hex::encode(&h.finalize()[..4])
    }
```

(`hex` 已是依赖,无需新增。)`default_for_test` / `for_test_with_port_offset` / `from_toml_str` **保持不变**。

3c. main.rs 调用点(锚点 `let config = localtrans_relay::RelayConfig::load_or_default(...)`)改为:

```rust
    let config = localtrans_relay::RelayConfig::load(std::path::Path::new(&config_path))?;
    tracing::info!("psk 摘要: {} (sha256 前 8 位, 供核对)", localtrans_relay::RelayConfig::psk_digest(&config.psk));
```

- [ ] **Step 4: 跑测试 + relay 回归**

Run: `cargo test -p localtrans-relay`
Expected: 新增 4 个 + 既有 e2e 全绿(e2e 走 for_test_with_port_offset,不受影响)

- [ ] **Step 5: 提交**

```bash
git add crates/localtrans-relay/src/config.rs crates/localtrans-relay/src/main.rs crates/localtrans-relay/Cargo.toml
git commit -m "fix(security): relay 缺配置/短 PSK 拒绝启动 + 启动日志 PSK 摘要(P0-3)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 5: 移除信任即时生效——fail-closed 权限 + 移除即断开(P0-5)

**Files:**
- Modify: `crates/localtrans-core/src/identity.rs`(Perms::denied)
- Modify: `crates/localtrans-core/src/transfer/engine.rs`(5 处 `unwrap_or_default` → fail-closed)
- Modify: `src-tauri/src/commands.rs`(remove_trusted 补 disconnect)
- Test: engine.rs tests mod

**Interfaces:**
- Produces: `identity::Perms::denied() -> Perms`(Task 6 消费)

- [ ] **Step 1: 写失败测试**

engine.rs tests mod 追加(harness 照抄邻近既有测试的双 SM 连接模式,基线 2636-2704 一带有完整示例;区别是 A 侧**不**起路由器,直接拿 A 的入站通道收响应):

```rust
#[tokio::test]
async fn removed_peer_fail_closed() {
    // A/B 互信连接后,B 移除 A 的信任 → A 的 SharesReq/ListReq 得空响应而非真实数据
    let (sm_a, ctx_a, sm_b, ctx_b, reg_b, fp_a, fp_b) = setup_connected_pair_with_share().await;
    let (ctrl_tx_a, ctrl_rx_a) = mpsc::channel(16);
    sm_a.set_inbound_ctrl_tx(ctrl_tx_a).await; // A 侧收响应(实现时以邻近测试的实际 API 为准:
                                                // 既有测试用 take_inbound_ctrl_rx 或注入通道,照抄其一)
    let (ask_tx, _ask_rx) = mpsc::channel(8);
    spawn_rpc_router(sm_b.clone(), ctx_b.clone(), reg_b.clone(), ctrl_rx_a_dummy_for_b_router(), ask_tx,
        crate::transfer::sender_state::new_sender_job_map(), None);

    // 移除信任(模拟用户点"移除配对")
    ctx_b.trust.lock().await.remove(&fp_a);

    // A 请求共享列表 → 必须空(fail-closed),而非 Perms::default() 的 browse=true
    sm_a.send_ctrl(&fp_b, ControlMsg::SharesReq {}).await.unwrap();
    let resp = recv_ctrl_with_timeout(&sm_a, std::time::Duration::from_secs(5)).await;
    match resp {
        ControlMsg::SharesResp { shares } => assert!(shares.is_empty(), "移除信任后必须回空列表"),
        other => panic!("期望 SharesResp, 实际 {:?}", other),
    }
    let _ = std::fs::remove_dir_all(share_root_of(&reg_b));
}
```

(注:`setup_connected_pair_with_share` / `recv_ctrl_with_timeout` / `ctrl_rx_a_dummy_for_b_router` 不是现成函数——实现时把本测试内联展开为邻近测试(基线 2636 `let (ask_tx, _ask_rx) = ...` 上下文)的完整连接步骤:B 起 router 用的 ctrl_rx 来自 `sm_b.take_inbound_ctrl_rx()`;A 的响应经 `sm_a` 的入站通道收取。**测试语义红线:移除信任后 SharesResp 必须为空**——连接细节允许适配既有 harness,语义不允许变。)

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p localtrans-core removed_peer`
Expected: FAIL——当前 `unwrap_or_default()` 回落 browse=true,返回真实 shares

- [ ] **Step 3: 实现**

3a. identity.rs `impl Default for Perms` 之后新增:

```rust
impl Perms {
    /// P0-5 fail-closed:不在信任表的对端一律全拒。
    /// 与 Default(browse/download 默认true,配对初始授权用)相反,仅用于查不到记录的回退。
    pub fn denied() -> Self {
        Perms { browse: false, download: false, push: PushPolicy::Deny }
    }
}
```

3b. engine.rs 五处权限查询(SharesReq 基线 1449 / ListReq 1475 / MetaReq 1512 / FetchReq 1652 / OfferReq 1733)统一把:

```rust
                        .unwrap_or_default();
```

改为:

```rust
                        .unwrap_or_else(crate::identity::Perms::denied);
```

(每处只改这一行;注意基线 2369 的 `unwrap_or_default()` 是 PathBuf 语境,**不是**权限查询,不要动。)

3c. commands.rs `remove_trusted`(基线 1742)在 `if removed {` 块内、save 之前加断开(照抄 reject_pairing 基线 172 的模式):

```rust
    if removed {
        // P0-5: 移除信任即时生效——立即断开既有会话(否则对端连接续命到自然超时)
        state.sm.disconnect(&fp).await;
        state.trust.lock().await.save().map_err(|e| e.to_string())?;
    }
```

- [ ] **Step 4: 跑测试 + 回归**

Run: `cargo test -p localtrans-core && cargo check -p localtrans-tauri 2>/dev/null || cargo check --manifest-path src-tauri/Cargo.toml`
Expected: removed_peer 通过;若既有测试失败,逐个检查——凡是"未插入信任却期望成功"的测试是测到了 fail-open 旧行为,修测试(补插信任)并在报告注明;正常 harness(先配对后操作)不受影响

- [ ] **Step 5: 提交**

```bash
git add crates/localtrans-core/src/identity.rs crates/localtrans-core/src/transfer/engine.rs src-tauri/src/commands.rs
git commit -m "fix(security): 移除信任即时生效——权限 fail-closed + 移除即断会话(P0-5)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 6: Share* 三分支权限门(P0-2a)

**Files:**
- Modify: `crates/localtrans-core/src/transfer/engine.rs`(ShareRename/ShareDelete/ShareMkdir 分支,基线 2005-2034)
- Test: engine.rs tests mod

**Interfaces:**
- Consumes: Task 5 `crate::identity::Perms::denied`

- [ ] **Step 1: 写失败测试**

engine.rs tests mod 追加(harness 同 Task 5 的连接模式,B 侧带一个含文件的共享区):

```rust
#[tokio::test]
async fn share_ops_require_browse_perm() {
    // B 把 A 降权(browse=false)→ rename/delete/mkdir 全部拒绝且文件原样
    // harness: 同 removed_peer_fail_closed 的连接步骤,区别:ctx_b.trust 里 upsert
    // TrustedPeer { fingerprint: fp_a, perms: Perms { browse: false, download: false, push: Deny }, .. }
    // 断言:对三个 Share* 各发一条,A 收到的 ShareOpResult.ok 全为 false 且 error 含 "无操作权限";
    //       共享区内文件未被改/删,目录未新建。
    // (实现时展开为完整代码,断言项如上逐条 assert;这是本测试的验收语义。)
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p localtrans-core share_ops_require`
Expected: FAIL——当前三分支无权限检查直接执行(ok=true)

- [ ] **Step 3: 实现**

三个分支(ShareRename/ShareDelete/ShareMkdir)各在 `let result = reg.op_...` 之前插入同一段门(以 ShareRename 为例,其余两个仅日志动词不同):

```rust
                ControlMsg::ShareRename { share_id, path, new_name } => {
                    // P0-2a: 远程文件操作权限门(与 ListReq 对齐要求 browse;fail-closed)
                    let perms = ctx
                        .trust
                        .lock()
                        .await
                        .get(&fingerprint)
                        .map(|p| p.perms.clone())
                        .unwrap_or_else(crate::identity::Perms::denied);
                    if !perms.browse {
                        tracing::warn!("{} 无操作权限, 拒绝 ShareRename", hex::encode(&fingerprint[..4]));
                        let _ = sm.send_ctrl(&fingerprint, ControlMsg::ShareOpResult {
                            ok: false, error: Some("无操作权限".into()),
                        }).await;
                        continue;
                    }
                    let result = reg.op_rename(&share_id, &path, &new_name)
                    // ...(原有代码不动)
```

同时把 ShareDelete 成功分支的 `tracing::debug!(share_id, "share op delete handled");` 升级为:

```rust
                    tracing::info!(share_id, "远程删除已执行: 对端={}", hex::encode(&fingerprint[..4]));
```

(仅成功路径;失败路径保持 debug。不含文件名——明文名不进日志规约。)

- [ ] **Step 4: 跑测试 + 回归**

Run: `cargo test -p localtrans-core`
Expected: 新测试过;既有 share_op 系测试(ffi/lib.rs 测试与 engine 测试)**若**走"已插入信任+browse=true"路径则不受影响

- [ ] **Step 5: 提交**

```bash
git add crates/localtrans-core/src/transfer/engine.rs
git commit -m "feat(security): ShareRename/Delete/Mkdir 权限门 + 删除审计日志升级(P0-2a)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 7: 删除确认门 DeleteAsk——core 层(P0-2b)

**Files:**
- Modify: `crates/localtrans-core/src/transfer/engine.rs`(DeleteAsk 结构、router 签名 + ShareDelete 改造、`count_dir_entries` 辅助;全部调用点补参:engine tests 约 7 处 + 后续 Task 8/9 的壳层)
- Modify: `crates/localtrans-core/src/transfer/mod.rs`(re-export DeleteAsk)

**Interfaces:**
- Consumes: Task 6 的权限门(在门之后追加确认流程)
- Produces:
  - `pub struct DeleteAsk { pub ask_id: u64, pub from: Fingerprint, pub share_id: String, pub name: String, pub is_dir: bool, pub entry_count: u64, pub respond: tokio::sync::oneshot::Sender<bool>, pub deadline_epoch_ms: i64 }`
  - `spawn_rpc_router` 新增第 5 参:`delete_ask_tx: mpsc::Sender<DeleteAsk>`(插在 `ask_tx` 之后)
  - `fn count_dir_entries(p: &Path) -> u64`(私有,上限 10000 截断)

- [ ] **Step 1: 写失败测试**

engine.rs tests mod 追加(4 个;harness 同 Task 5/6,B 侧 router 的 delete_ask_tx 接到测试持有的 mpsc receiver,consent_timeout_secs 设为 1 秒供超时用例):

```rust
// 1) delete_confirm_true_executes: respond(true) → ShareOpResult ok=true 且目录被删
// 2) delete_confirm_false_keeps: respond(false) → ok=false error 含 "未确认" 且目录原样
// 3) delete_confirm_timeout_denies: 不 respond,等 2s → ok=false(超时自动拒绝),进程/会话存活
// 4) delete_pending_cap_rejects: 连发 9 个 ShareDelete(不 respond)→ 第 9 个立即 ok=false error 含 "稍后"
// 5) router_not_blocked_during_pending: 挂起一个未响应的删除期间,A 发 ListReq 仍能收到 ListResp
```

(实现时展开为完整代码;5 个用例的断言语义如上,红线:3)和 5)证明等待在子任务中、路由器不阻塞。)

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p localtrans-core delete_confirm`
Expected: 编译失败(DeleteAsk 不存在)

- [ ] **Step 3: 实现**

3a. engine.rs 在 `OfferAsk` 结构(基线 406)之后新增:

```rust
/// P0-2b: 远程删除确认事件(仿 OfferAsk;仅本机进程内,不走网络协议)
pub struct DeleteAsk {
    pub ask_id: u64,
    pub from: Fingerprint,
    pub share_id: String,
    /// 相对路径末段,仅本机 UI 展示(不进日志)
    pub name: String,
    pub is_dir: bool,
    /// 目录时的条目数(给用户判断依据;上限 10000 截断)
    pub entry_count: u64,
    pub respond: tokio::sync::oneshot::Sender<bool>,
    /// 复用 consent_timeout_secs(默认 60s);超时=自动拒绝
    pub deadline_epoch_ms: i64,
}
```

3b. `count_dir_entries` 私有辅助(放 sanitize_component 附近):

```rust
/// 目录条目计数(含子目录递归),上限 10000 截断——防共享区超大树卡住路由器
fn count_dir_entries(root: &Path) -> u64 {
    fn walk(dir: &Path, count: &mut u64) {
        if *count >= 10_000 { return; }
        if let Ok(entries) = std::fs::read_dir(dir) {
            for e in entries.flatten() {
                *count += 1;
                if *count >= 10_000 { return; }
                let p = e.path();
                if p.is_dir() {
                    walk(&p, count);
                }
            }
        }
    }
    let mut n = 0u64;
    if root.is_dir() { walk(root, &mut n); }
    n
}
```

3c. `spawn_rpc_router` 签名在 `ask_tx: mpsc::Sender<OfferAsk>,` 之后插一行 `delete_ask_tx: mpsc::Sender<DeleteAsk>,`;函数体 tokio::spawn 内开头加:

```rust
        let mut next_ask_id: u64 = 1;
        // 待确认删除并发上限 8(防弹窗轰炸;超出直接拒绝)
        let delete_permits = Arc::new(tokio::sync::Semaphore::new(8));
```

ShareDelete 分支(Task 6 权限门之后)整段替换为:

```rust
                ControlMsg::ShareDelete { share_id, path } => {
                    // ...(Task 6 的权限门保持在前)...
                    // 解析 + 统计(失败即回错误,不进确认流)
                    let resolved = match reg.resolve(&share_id, &path) {
                        Ok(p) => p,
                        Err(e) => {
                            let _ = sm.send_ctrl(&fingerprint, ControlMsg::ShareOpResult {
                                ok: false, error: Some(e.to_string()),
                            }).await;
                            continue;
                        }
                    };
                    let is_dir = resolved.is_dir();
                    let entry_count = if is_dir { count_dir_entries(&resolved) } else { 0 };
                    let name = path.rsplit('/').next().unwrap_or(&path).to_string();

                    let permit = match delete_permits.clone().try_acquire_owned() {
                        Ok(p) => p,
                        Err(_) => {
                            let _ = sm.send_ctrl(&fingerprint, ControlMsg::ShareOpResult {
                                ok: false, error: Some("待确认删除过多, 请稍后再试".into()),
                            }).await;
                            continue;
                        }
                    };

                    let secs = ctx.config.read().await.consent_timeout_secs.clamp(1, 600);
                    let deadline_epoch_ms = std::time::SystemTime::now()
                        .duration_since(std::time::UNIX_EPOCH)
                        .map(|d| d.as_millis() as i64 + (secs as i64) * 1000)
                        .unwrap_or(0);
                    let ask_id = next_ask_id;
                    next_ask_id += 1;
                    let (tx, rx) = tokio::sync::oneshot::channel::<bool>();
                    // fail-closed:壳层无消费者/已退出 → send 失败 → 拒绝
                    if delete_ask_tx.send(DeleteAsk {
                        ask_id, from: fingerprint, share_id: share_id.clone(),
                        name, is_dir, entry_count, respond: tx, deadline_epoch_ms,
                    }).await.is_err() {
                        let _ = sm.send_ctrl(&fingerprint, ControlMsg::ShareOpResult {
                            ok: false, error: Some("对方未确认删除".into()),
                        }).await;
                        continue;
                    }

                    // 等待移入子任务:路由器循环绝不 await 用户响应
                    let sm_c = sm.clone();
                    let reg_c = reg.clone();
                    let fp_c = fingerprint;
                    tokio::spawn(async move {
                        let deadline = tokio::time::Instant::now()
                            + std::time::Duration::from_secs(secs);
                        let allowed = match tokio::time::timeout_at(deadline, rx).await {
                            Ok(Ok(true)) => true,
                            _ => false, // 拒绝 / 通道关闭 / 超时 —— 全部 fail-closed
                        };
                        drop(permit);
                        let result = if allowed {
                            reg_c.op_delete(&share_id, &path).map_err(|e| e.to_string())
                        } else {
                            Err("对方未确认删除".to_string())
                        };
                        let ok = result.is_ok();
                        let _ = sm_c.send_ctrl(&fp_c, ControlMsg::ShareOpResult {
                            ok, error: result.err(),
                        }).await;
                        if ok {
                            tracing::info!(share_id, "远程删除已确认并执行: 对端={}", hex::encode(&fp_c[..4]));
                        }
                    });
                    continue;
                }
```

(`Arc` 已在文件 use;`fingerprint` 是 `[u8;32]` Copy 类型,直接 move。)

3d. **全部既有调用点补参**(engine tests 约 7 处 + 无壳层——壳层在 Task 8/9):在每处 `spawn_rpc_router(...)` 的 `ask_tx` 实参之后插入:

```rust
                mpsc::channel(8).0,  // 测试无删除确认消费者:send 失败 → fail-closed 拒绝
```

(若该测试本身要验证删除,则显式 `let (delete_ask_tx, mut delete_ask_rx) = mpsc::channel(8);` 传入——Step 1 的新测试如此。)

3e. mod.rs 的 pub use engine::{...} 列表加 `DeleteAsk`。

- [ ] **Step 4: 跑测试 + 回归**

Run: `cargo test -p localtrans-core`
Expected: 新增 5 个通过;既有测试因补参后行为不变(browse=true 路径的删除测试现在会收到 ok=false "对方未确认删除"——**若既有测试直接调 op 语义的删除断言,改为经由 delete_ask_rx 自动 respond(true)**;报告中列出改动的测试)

- [ ] **Step 5: 提交**

```bash
git add crates/localtrans-core/src/transfer/engine.rs crates/localtrans-core/src/transfer/mod.rs
git commit -m "feat(security): 远程删除确认门 DeleteAsk——权限门后经本机确认才执行(P0-2b)

子任务等待不阻塞路由器;并发上限 8;超时/无消费者 fail-closed。

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 8: 桌面壳删除确认——事件 + 命令 + 模态(P0-2c)

**Files:**
- Modify: `src-tauri/src/main.rs`(delete_ask_rx 循环 + 事件 emit + spawn_rpc_router 补参)
- Modify: `src-tauri/src/commands.rs`(AppState 加 pending_deletes;respond_delete 命令)
- Modify: `ui/src/api.ts`(onDeleteRequest / respondDelete)
- Modify: `ui/src/App.vue`(确认模态,复用 offer-modal CSS 与 useOfferCountdown)

**Interfaces:**
- Consumes: Task 7 `DeleteAsk` + spawn_rpc_router 新签名
- Produces: 事件 `delete-request` payload `{ ask_id, fingerprint, peer_name, share_id, name, is_dir, entry_count, deadline_epoch_ms }`;命令 `respond_delete(ask_id: u64, allow: bool)`

- [ ] **Step 1: 壳层接线(无新 Rust 单测;桌面壳历来靠 core 测 + 手测)**

1a. commands.rs:AppState struct 加字段(与 pending_offers 并列):

```rust
    pub pending_deletes: tokio::sync::Mutex<HashMap<u64, tokio::sync::oneshot::Sender<bool>>>,
```

构造处同步初始化 `pending_deletes: tokio::sync::Mutex::new(HashMap::new()),`。

1b. commands.rs 新命令(放 respond_offer 附近):

```rust
/// P0-2c: 应答远程删除确认
#[tauri::command]
pub async fn respond_delete(
    state: State<'_, AppState>,
    ask_id: u64,
    allow: bool,
) -> Result<(), String> {
    if let Some(tx) = state.pending_deletes.lock().await.remove(&ask_id) {
        let _ = tx.send(allow);
    }
    Ok(())
}
```

并在 main.rs 的 invoke_handler 注册 `commands::respond_delete`。

1c. main.rs:spawn_rpc_router 调用处(基线 487 上下文)之前建通道、传参:

```rust
                let (delete_ask_tx, mut delete_ask_rx) = mpsc::channel::<localtrans_core::transfer::DeleteAsk>(8);
```

spawn_rpc_router 的 `ask_tx,` 之后插 `delete_ask_tx,`。

之后新增事件循环(仿 ask_rx 循环,基线 923 一带):

```rust
                // delete_ask_rx 循环:远程删除确认 → 前端模态
                let app_handle_d = app_handle.clone();
                let trust_for_d = trust.clone();
                tokio::spawn(async move {
                    while let Some(ask) = delete_ask_rx.recv().await {
                        if let Some(st) = app_handle_d.try_state::<AppState>() {
                            st.pending_deletes.lock().await.insert(ask.ask_id, ask.respond);
                            // 超时看门狗:deadline+3s 清残留(镜像 offer 看门狗)
                            let wd = app_handle_d.clone();
                            let (aid, dl) = (ask.ask_id, ask.deadline_epoch_ms);
                            tokio::spawn(async move {
                                let now = std::time::SystemTime::now()
                                    .duration_since(std::time::UNIX_EPOCH).unwrap_or_default();
                                let wait = (dl as u64 + 3000).saturating_sub(now.as_millis() as u64);
                                tokio::time::sleep(std::time::Duration::from_millis(wait)).await;
                                if let Some(st) = wd.try_state::<AppState>() {
                                    st.pending_deletes.lock().await.remove(&aid);
                                }
                            });
                        }
                        let peer_name = trust_for_d.lock().await
                            .get(&ask.from)
                            .map(|p| if p.alias.is_empty() { p.name.clone() } else { p.alias.clone() })
                            .unwrap_or_else(|| "未知设备".into());
                        let _ = app_handle_d.emit("delete-request", serde_json::json!({
                            "ask_id": ask.ask_id,
                            "fingerprint": hex::encode(ask.from),
                            "peer_name": peer_name,
                            "share_id": ask.share_id,
                            "name": ask.name,
                            "is_dir": ask.is_dir,
                            "entry_count": ask.entry_count,
                            "deadline_epoch_ms": ask.deadline_epoch_ms,
                        }));
                    }
                });
```

(trust 的类型若与 ctx.trust 同源(Arc<tokio::sync::Mutex<TrustStore>>)直接用;若 main.rs 作用域里叫别的名字,以实际为准。)

- [ ] **Step 2: 前端 api.ts(offer-request 封装之后追加)**

```typescript
/** 远程删除确认事件 */
export function onDeleteRequest(callback: (req: {
  ask_id: number; fingerprint: string; peer_name: string; share_id: string;
  name: string; is_dir: boolean; entry_count: number; deadline_epoch_ms: number;
}) => void) {
  return onEvent('delete-request', callback)
}

/** 应答远程删除确认 */
export function respondDelete(askId: number, allow: boolean) {
  invokeCommand('respond_delete', { askId, allow })
}
```

- [ ] **Step 3: App.vue 模态**

3a. script setup 区(offerRequest ref 旁):

```typescript
interface DeleteAskEvent {
  ask_id: number; fingerprint: string; peer_name: string; share_id: string;
  name: string; is_dir: boolean; entry_count: number; deadline_epoch_ms: number;
}
const deleteAsk = ref<DeleteAskEvent | null>(null)
const deleteCountdown = useOfferCountdown(computed(() => deleteAsk.value?.deadline_epoch_ms ?? null))
let deleteUnlisten: (() => void) | null = null
onMounted(async () => {
  deleteUnlisten = onDeleteRequest((req) => { deleteAsk.value = req })
})
onUnmounted(() => { deleteUnlisten?.() })
function handleDeleteRespond(allow: boolean) {
  if (!deleteAsk.value) return
  respondDelete(deleteAsk.value.ask_id, allow)
  deleteAsk.value = null
}
```

(import 区补 `onDeleteRequest, respondDelete` 与 `onUnmounted`;`useOfferCountdown`/`computed`/`onMounted` 已有引用模式照抄。)

3b. template 区(关闭拦截模态之前插入;CSS 类复用 offer-modal 系,新增的 desc 行用既有 `.file-meta` 风格):

```html
    <!-- 远程删除确认模态 -->
    <div v-if="deleteAsk" class="offer-modal-overlay" @click="handleDeleteRespond(false)">
      <div class="offer-modal" @click.stop>
        <div class="modal-header">
          <h3>删除确认</h3>
          <button @click="handleDeleteRespond(false)" class="btn-close">×</button>
        </div>
        <div v-if="deleteCountdown.remainingMs.value > 0" class="offer-countdown"
          :class="{ urgent: deleteCountdown.urgent.value }" data-testid="delete-countdown">
          {{ Math.floor(deleteCountdown.remainingMs.value / 60000) }}:{{ String(Math.floor((deleteCountdown.remainingMs.value % 60000) / 1000)).padStart(2, '0') }} 后自动拒绝
        </div>
        <div class="modal-body">
          <div class="offer-info">
            <div class="info-item">
              <span class="info-label">请求设备:</span>
              <span class="info-value">{{ deleteAsk.peer_name }}</span>
            </div>
            <div class="info-item">
              <span class="info-label">将删除:</span>
              <span class="info-value">{{ deleteAsk.name }}{{ deleteAsk.is_dir ? `(文件夹,共 ${deleteAsk.entry_count} 项)` : '' }}</span>
            </div>
          </div>
        </div>
        <div class="modal-footer">
          <button @click="handleDeleteRespond(false)" class="btn btn-secondary">拒绝</button>
          <button @click="handleDeleteRespond(true)" class="btn btn-danger">允许删除</button>
        </div>
      </div>
    </div>
```

(`btn-danger` 若无定义,用 `btn btn-primary` 代替并在报告注明。)

- [ ] **Step 4: 编译验证**

Run: `cargo check --manifest-path src-tauri/Cargo.toml` 与 `cd ui && npm run build`(或既有 build 脚本)
Expected: 双绿;vitest 既有 3 个 TransferItem 失败为存量,不算新增失败

- [ ] **Step 5: 提交**

```bash
git add src-tauri/src/main.rs src-tauri/src/commands.rs ui/src/api.ts ui/src/App.vue
git commit -m "feat(security): 桌面端远程删除确认模态 + respond_delete 命令(P0-2c)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 9: Android 壳删除确认——FFI 事件 + Kotlin 弹窗(P0-2d)+ 失败文案

**Files:**
- Modify: `crates/localtrans-ffi/src/lib.rs`(AppEvent::DeleteRequested、delete_ask_rx 循环、respond_delete、spawn_rpc_router 补参)
- Modify: `crates/localtrans-ffi/src/state.rs`(pending_deletes 字段)
- Modify: `android/app/src/main/java/com/localtrans/app/ui/nav/AppNav.kt`(全局确认弹窗)
- Modify: `android/app/src/main/java/com/localtrans/app/ui/transfers/FailReasons.kt` + 测试(非法文件名文案)

**Interfaces:**
- Consumes: Task 7 `DeleteAsk` + spawn_rpc_router 新签名
- Produces: FFI `AppEvent::DeleteRequested { ask_id: u64, peer_name: String, name: String, is_dir: bool, entry_count: u64, deadline_epoch_ms: i64 }`;FFI 方法 `pub fn respond_delete(&self, ask_id: u64, allow: bool) -> Result<(), AppException>`

- [ ] **Step 1: FFI 层**

1a. state.rs:state struct 加 `pub pending_deletes: Arc<TokioMutex<HashMap<u64, tokio::sync::oneshot::Sender<bool>>>>`(与 pending_offers 并列,构造处初始化)。

1b. lib.rs AppEvent 枚举加(OfferRequested 之后):

```rust
    /// P0-2d: 远程删除确认请求(本机是共享区持有方)
    DeleteRequested { ask_id: u64, peer_name: String, name: String, is_dir: bool, entry_count: u64, deadline_epoch_ms: i64 },
```

1c. lib.rs `start()` 内:spawn_rpc_router 调用处(基线 292)之前建 `let (delete_ask_tx, mut delete_ask_rx) = mpsc::channel(8);`,调用补参 `delete_ask_tx,`;新增循环(仿 ask_rx 循环,基线 306):

```rust
            let state_for_del = state.clone();
            let trust_for_del = trust.clone();
            tokio::spawn(async move {
                while let Some(ask) = delete_ask_rx.recv().await {
                    state_for_del.pending_deletes.lock().await.insert(ask.ask_id, ask.respond);
                    // 看门狗:deadline+3s 清残留(镜像 offer 看门狗模式)
                    let st_wd = state_for_del.clone();
                    let (aid, dl) = (ask.ask_id, ask.deadline_epoch_ms);
                    tokio::spawn(async move {
                        let now = std::time::SystemTime::now()
                            .duration_since(std::time::UNIX_EPOCH).unwrap_or_default();
                        let wait = (dl as u64 + 3000).saturating_sub(now.as_millis() as u64);
                        tokio::time::sleep(std::time::Duration::from_millis(wait)).await;
                        st_wd.pending_deletes.lock().await.remove(&aid);
                    });
                    let peer_name = trust_for_del.lock().await
                        .get(&ask.from)
                        .map(|p| if p.alias.is_empty() { p.name.clone() } else { p.alias.clone() })
                        .unwrap_or_else(|| "未知设备".into());
                    if let Some(cb) = state_for_del.callback.upgrade() {
                        cb.on_event(AppEvent::DeleteRequested {
                            ask_id: ask.ask_id, peer_name,
                            name: ask.name, is_dir: ask.is_dir,
                            entry_count: ask.entry_count, deadline_epoch_ms: ask.deadline_epoch_ms,
                        });
                    }
                }
            });
```

(callback 获取方式以 ask_rx 循环实际写法为准照抄——lib.rs 基线 306-355 有完整参照。)

1d. lib.rs 新 FFI 方法(仿 respond_offer 基线 1075):

```rust
    /// P0-2d: 应答远程删除确认
    pub fn respond_delete(&self, ask_id: u64, allow: bool) -> Result<(), AppException> {
        let state = self.state.lock().map_err(|e| AppException::new(format!("锁中毒: {}", e)))?;
        // respond_offer 同款阻塞模式:看其实现是直接 take 还是一次性任务,照抄
        if let Some(tx) = state.pending_deletes.blocking_lock().remove(&ask_id) {
            let _ = tx.send(allow);
        }
        Ok(())
    }
```

(以 respond_offer 的实际锁/错误处理模式为准逐行对齐——它是最近的同型实现。)

1e. FailReasons.kt 映射表加一行(`isRetryableFailure` 之上):

```kotlin
    reason.contains("非法文件名") -> "文件名含目标系统不允许的字符"
```

FailReasonsTest.kt 补 1 用例:`localizeFailReason("非法文件名(含分隔符或非法字符)")` 返回该中文且 `isRetryableFailure` 对其返回 false。

- [ ] **Step 2: Kotlin 弹窗(AppNav.kt)**

AppNav 根组合函数内(照抄 EventRouter.events 的既有 collect 模式):

```kotlin
var deleteAsk by remember { mutableStateOf<uniffi.localtrans_ffi.AppEvent.DeleteRequested?>(null) }
LaunchedEffect(Unit) {
    EventRouter.events.collect { event ->
        if (event is uniffi.localtrans_ffi.AppEvent.DeleteRequested) deleteAsk = event
    }
}
deleteAsk?.let { ask ->
    AlertDialog(
        onDismissRequest = { respondDeleteLocal(ask.askId, false).also { deleteAsk = null } },
        title = { Text("删除确认") },
        text = {
            Text(
                "${ask.peerName} 请求删除共享文件夹中的「${ask.name}」" +
                    (if (ask.isDir) "(文件夹,共 ${ask.entryCount} 项)" else "")
            )
        },
        confirmButton = {
            TextButton(onClick = { respondDeleteLocal(ask.askId, true).also { deleteAsk = null } }) { Text("允许删除") }
        },
        dismissButton = {
            TextButton(onClick = { respondDeleteLocal(ask.askId, false).also { deleteAsk = null } }) { Text("拒绝") }
        }
    )
}
```

辅助函数(AppNav 内或 bridge 包,uniffi 对象获取方式照抄 TransfersRepo.respondOffer 的 `app` 获取):

```kotlin
private fun respondDeleteLocal(askId: ULong, allow: Boolean) {
    try {
        LocalTransBridge.getInstance().respondDelete(askId.toLong(), allow)
    } catch (e: Exception) {
        // Bridge 未初始化时静默——fail-closed 由 core 兜底(超时自动拒绝)
    }
}
```

(uniffi 生成的 ask_id 是 u64→kotlin ULong,respond_delete 参数以生成绑定实际类型为准对齐。)

- [ ] **Step 3: 重建 so + bindings + 测试**

1. so/bindings 重建命令以仓库文档与上次生成方式为准:`git log --oneline -- android/app/src/main/jniLibs` 与 `docs/` 下构建文档(先 `ls docs` 找 android/构建相关 md);典型链:`cargo ndk`(目标 ABI 与输出目录照文档)→ `cargo run -p localtrans-ffi --bin uniffi-bindgen -- generate ...`(参数照文档)→ 拷贝生成的 kotlin 到 `android/app/src/main/java/uniffi/`
2. Run(PowerShell/GitBash):
```bash
export JAVA_HOME="C:/Users/<user>/Desktop/work/deepseek_use/tools/jdk17/jdk-17.0.20+8"
C:/Users/<user>/Desktop/work/deepseek_use/tools/gradle-8.10.2/bin/gradle.bat -p android :app:testReleaseUnitTest --offline
```
Expected: 既有 49+ 用例 + FailReasons 新用例全绿

- [ ] **Step 4: cargo 侧回归 + 提交**

Run: `cargo test -p localtrans-core && cargo test -p localtrans-ffi`(若 ffi 无测试目录则 `cargo check -p localtrans-ffi`)
```bash
git add crates/localtrans-ffi crates/localtrans-core/src 2>/dev/null; git add android/app/src/main/java android/app/src/main/jniLibs
git commit -m "feat(security): 安卓端远程删除确认弹窗 + FFI respond_delete + 非法文件名中文文案(P0-2d)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 10: 收尾——版本 v0.8.2 + CHANGELOG + 全量回归

**Files:**
- Modify: `Cargo.toml`(workspace version)、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json`(三处 0.8.1→0.8.2)
- Modify: `android/app/build.gradle.kts`(versionCode 6→7,versionName "0.8.1"→"0.8.2")
- Modify: `CHANGELOG.md`

- [ ] **Step 1: 版本五处对齐**(全部 0.8.1→0.8.2 / 6→7)

- [ ] **Step 2: CHANGELOG 顶部加小节**

```markdown
## v0.8.2(安全加固)

本版为安全修复版,协议无变更,旧版本对端不受影响;**接收侧升级即获保护**。

- 修复:推送文件名路径穿越(小文件批流 + 大文件 MetaResp 两条落盘链路统一净化,拒绝绝对路径/`..`/NTFS ADS/Windows 保留名/尾点)
- 修复:批流声明 size 无上限导致接收进程 OOM 崩溃(上限 1MiB)
- 修复:大文件 total_size 与 Offer 声明不一致时拒绝接收(防 part.bin 恶意扩展)
- 新增:远程删除共享区文件需本机确认(60s 超时自动拒绝;双端弹窗)
- 新增:远程重命名/删除/新建文件夹需浏览权限(降权设备不再能操作)
- 修复:移除信任后既有会话不断开且权限反升为默认全开的严重问题(移除即断开 + 全部权限查询 fail-closed)
- 修复:中继服务端配置缺失时静默回退 dev-psk 监听公网(改为拒绝启动;PSK 最短 16 字符)
- 修复:子目录同名冲突文件被改名后落回根目录的问题
```

- [ ] **Step 3: 全量回归**

```bash
cargo test --workspace
export JAVA_HOME="C:/Users/<user>/Desktop/work/deepseek_use/tools/jdk17/jdk-17.0.20+8"
C:/Users/<user>/Desktop/work/deepseek_use/tools/gradle-8.10.2/bin/gradle.bat -p android :app:testReleaseUnitTest --offline
cd ui && npm run build
```
Expected: cargo 全绿;安卓单测全绿;ui 构建成功(vitest 存量 3 失败不在此链)

- [ ] **Step 4: 提交**

```bash
git add -A
git commit -m "chore: v0.8.2 安全加固版收尾(版本五处 + CHANGELOG + 全量回归)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

(dist 打包与 tag 由控制器在用户确认后执行,不在本计划内)

---

## Self-Review 记录

- **Spec 覆盖**:P0-1(Task 1/2/3)、P0-2a(Task 6)、P0-2b(Task 7)、P0-2c/d(Task 8/9)、P0-3(Task 4)、P0-4(Task 2)、P0-5(Task 5)——五项全覆盖;FailReasons 中文化(spec P0-1 兼容性节)在 Task 9。
- **占位符扫描**:Task 5/6/7 的测试用"验收语义 + harness 展开指引"而非完整代码——因为双 SM 连接 harness 依赖 engine.rs 内既有测试基建(setup 步骤 200+ 行),逐字重写必然与实际 API 漂移;指引给出了照抄锚点(基线 2636/3318)与语义红线,实现者无法跳过断言。其余任务代码完整。
- **类型一致性**:DeleteAsk 字段在 Task 7(定义)/Task 8(JSON payload)/Task 9(AppEvent)三处一致(ask_id/from(fingerprint)/name/is_dir/entry_count/deadline_epoch_ms);respond_delete 签名桌面 (u64,bool)、FFI (u64,bool)→kotlin (Long/ULong 以绑定生成为准,已注记)。
- **执行顺序依赖**:1→2→3(函数→接入);5→6→7(权限门→确认门);7→8/9(签名);10 收尾。4 独立。并行派发时只可 (1,4) 或 (4, 5) 同批,其余串行。
