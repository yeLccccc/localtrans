# v0.10.2 缺陷修复批次 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复用户实测 7 项问题——隐身失效(中继名册+主动探测)、PC 两处 UI 布局、PC 配对卡片刷新滞后、Android 配对弹窗不关闭、Android ANR 卡死、长按菜单补发送入口。

**Architecture:** core 三处修复(relay Register.hidden 协议+服务端名册剔除、RelayClient 上报、discovery 探测门控、配对先回包后收尾)+ 双壳接线(set_hidden 触发中继重连、PairingResult 即时刷新)+ 纯 CSS 两处 + Android 三项(配对超时兜底、Repo suspend 化下 IO、长按发送)。

**Tech Stack:** Rust(tokio/quinn/serde)、Vue3+CSS、Kotlin Compose+uniFFI、Gradle/JDK17。

## Global Constraints

- 版本:workspace `Cargo.toml`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json` 三处 `0.10.2`;`android/app/build.gradle.kts` `versionCode = 13` / `versionName = "0.10.2"`;CHANGELOG 条目
- relay 协议 `Register.hidden` 向后兼容:`#[serde(default)]`,旧 server 忽略未知字段、旧 client 不上报即 false
- 安全规约:PSK 只打摘要;配对码/明文文件名不进日志与协议;dist zip 不含 data/
- 提交信息:中文前缀 + 空行 + `Co-Authored-By: Claude <noreply@anthropic.com>`
- Rust 测试 per-crate `-- --test-threads=1`;main 分支直接工作
- 命令行是 Windows Git Bash;gradle 用 `cmd //c "set JAVA_HOME=C:/Users/<user>\Desktop\work\deepseek_use\tools\jdk17\jdk-17.0.20+8&& C:/Users/<user>\Desktop\work\deepseek_use\tools\gradle-8.10.2\bin\gradle.bat -p C:/Users/<user>\Desktop\work\localTrans\android <task>"`(输出重定向文件再 tail)

---

### Task 1: relay 协议 Register.hidden + 服务端名册剔除(问题 01 服务端半)

**Files:**
- Modify: `crates/localtrans-core/src/relay/proto.rs:41`(Register 变体)、`proto.rs` tests(roundtrip+兼容)
- Modify: `crates/localtrans-relay/src/lease.rs:18-28`(LeaseEntry)、`lease.rs:63`(alloc)、`lease.rs:157-168`(snapshot_roster)、`lease.rs` tests
- Modify: `crates/localtrans-relay/src/control.rs:268-270`(Register handler)、`control.rs` tests(现有 Register 调用补参+新 E2E)

**Interfaces:**
- Produces: `RelayMsg::Register { name: String, fingerprint: [u8; 32], hidden: bool }`(serde default false);`LeaseTable::alloc(fp: [u8;32], name: String, hidden: bool) -> Option<AllocatedLease>`——Task 2 的 client 上报依赖此协议字段

- [ ] **Step 1: 写失败测试**

`crates/localtrans-core/src/relay/proto.rs` tests 模块追加:

```rust
#[test]
fn register_without_hidden_decodes_as_visible() {
    // 旧客户端 Register 不带 hidden 字段 → 反序列化为 false(向后兼容)
    let full = RelayMsg::Register { name: "旧端".into(), fingerprint: [1u8; 32], hidden: true };
    let v = serde_json::to_value(&full).unwrap();
    let mut stripped = v.clone();
    stripped.as_object_mut().unwrap().remove("hidden");
    let body = stripped.to_string();
    let mut framed = Vec::new();
    framed.extend_from_slice(&(body.len() as u32).to_be_bytes());
    framed.extend_from_slice(body.as_bytes());
    match decode_relay_msg(&framed).unwrap() {
        RelayMsg::Register { hidden, .. } => assert!(!hidden, "缺省 hidden 应为 false"),
        other => panic!("期望 Register, 实得 {:?}", other),
    }
}
```

`crates/localtrans-relay/src/lease.rs` tests 模块追加:

```rust
#[test]
fn snapshot_roster_excludes_hidden() {
    let table = LeaseTable::new(19000..19100);
    table.alloc([1u8; 32], "可见".into(), false).unwrap();
    table.alloc([2u8; 32], "隐身".into(), true).unwrap();
    let roster = table.snapshot_roster(&[9u8; 32], "1.2.3.4".parse().unwrap());
    assert!(roster.iter().any(|d| d.fingerprint == [1u8; 32]), "可见设备应在名册");
    assert!(!roster.iter().any(|d| d.fingerprint == [2u8; 32]), "隐身设备不应在名册");
}
```

`crates/localtrans-relay/src/control.rs` tests 模块追加(仿 `roster_pushes_on_second_registration` 风格):

```rust
#[tokio::test]
#[serial]
async fn hidden_client_excluded_from_roster() {
    let config = RelayConfig::for_test_with_port_offset(4);
    let server = RelayServer::bind(config).await.unwrap();
    let port = server.local_control_addr().port();
    let addr = format!("127.0.0.1:{}", port).parse().unwrap();
    tokio::spawn(server.clone().run());

    // A 正常注册
    let conn_a = client_ep().connect(addr, "localtrans-relay").unwrap().await.unwrap();
    psk_hello(&conn_a, "dev-psk").await.unwrap();
    send_msg(&conn_a, &RelayMsg::Register { name: "A".into(), fingerprint: [1u8; 32], hidden: false }).await.unwrap();
    let _ = recv_msg(&conn_a).await.unwrap(); // RegisterAck

    // B 隐身注册
    let conn_b = client_ep().connect(addr, "localtrans-relay").unwrap().await.unwrap();
    psk_hello(&conn_b, "dev-psk").await.unwrap();
    send_msg(&conn_b, &RelayMsg::Register { name: "B".into(), fingerprint: [2u8; 32], hidden: true }).await.unwrap();
    let _ = recv_msg(&conn_b).await.unwrap(); // RegisterAck

    // B 注册触发的广播:A 收到 Roster,不含 B(隐身被剔除)
    match recv_msg(&conn_a).await.unwrap() {
        RelayMsg::Roster { devices, .. } => {
            assert!(!devices.iter().any(|d| d.fingerprint == [2u8; 32]), "名册不应包含隐身设备");
        }
        other => panic!("期望 Roster, 实得 {:?}", other),
    }

    // B 发 Ping 拉名册:能看到 A(隐身者自己仍可见名册——半隐身语义)
    send_msg(&conn_b, &RelayMsg::Ping).await.unwrap();
    match recv_msg(&conn_b).await.unwrap() {
        RelayMsg::Roster { devices, .. } => {
            assert!(devices.iter().any(|d| d.fingerprint == [1u8; 32]), "隐身设备应能收到名册");
        }
        other => panic!("期望 Roster, 实得 {:?}", other),
    }

    server.shutdown().await;
}
```

同时把 proto.rs 现有 `relay_msg_roundtrip` 里 Register 字面量补 `hidden: true`,lease.rs/control.rs 现有测试的 `alloc(...)`/`Register{...}` 调用按新签名补 `false`(编译期暴露全部调用点)。

- [ ] **Step 2: 跑测试确认失败**

```bash
cargo test -p localtrans-core relay::proto -- --test-threads=1
cargo test -p localtrans-relay -- --test-threads=1
```
Expected: 编译失败(Register 无 hidden 字段/alloc 参数不符)——这就是红。

- [ ] **Step 3: 实现**

`proto.rs:41`:
```rust
Register { name: String, fingerprint: [u8; 32], #[serde(default)] hidden: bool },
```

`lease.rs` LeaseEntry 加字段:
```rust
pub struct LeaseEntry {
    pub fingerprint: [u8; 32],
    pub name: String,
    pub port: u16,
    pub token: [u8; 16],
    pub last_active: Instant,
    pub punch_from: Option<SocketAddr>,
    pub last_ping: Instant,
    /// 隐身注册:名册剔除本设备(连接/打洞能力保留)
    pub hidden: bool,
}
```
alloc 签名与两处赋值:
```rust
pub fn alloc(&self, fp: [u8; 32], name: String, hidden: bool) -> Option<AllocatedLease> {
    // 已有租约路径内:
    //   existing.name = name; existing.hidden = hidden;  (重连时隐身状态刷新)
    // 新租约构造:leases.insert(fp, LeaseEntry { ..., hidden });
}
```
snapshot_roster 过滤:
```rust
leases.iter()
    .filter(|(fp, e)| *fp != exclude_fp && !e.hidden)
```

`control.rs:268` handler:
```rust
RelayMsg::Register { name, fingerprint, hidden } => {
    let allocated = self.leases.alloc(fingerprint, name.clone(), hidden);
    ...(其余不变)
```

- [ ] **Step 4: 跑测试转绿+回归**

```bash
cargo test -p localtrans-core -- --test-threads=1 && cargo test -p localtrans-relay -- --test-threads=1
```
Expected: 全绿。

- [ ] **Step 5: Commit**

```bash
git add crates/localtrans-core/src/relay/proto.rs crates/localtrans-relay/src
git commit -m "fix(relay): Register 上报隐身标志,服务端名册剔除隐身设备

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 2: RelayClient 上报 hidden + 双壳三处构造点(问题 01 客户端半)

**Files:**
- Modify: `crates/localtrans-core/src/relay/client.rs:19-24`(RelayClientConfig)、`client.rs:143-146`(Register 构造)
- Modify: `crates/localtrans-ffi/src/relay_state.rs:34-37,73-77`(FFI 构造点)
- Modify: `src-tauri/src/main.rs:584-589,603-607`(PC 启动构造点)
- Modify: `src-tauri/src/commands.rs:1913-1919`(PC set_relay_config 构造点)

**Interfaces:**
- Consumes: Task 1 的 `RelayMsg::Register { .., hidden }`
- Produces: `RelayClientConfig { server_addr, psk, device_name, hidden: bool }`——Task 4 依赖

- [ ] **Step 1: 改配置结构与上报**

`client.rs`:
```rust
pub struct RelayClientConfig {
    pub server_addr: SocketAddr,
    pub psk: String,
    /// Register 上报的显示名(空串时兜底 "LocalTrans")
    pub device_name: String,
    /// 隐身:注册上报,服务端名册剔除本设备(半隐身:可看名册可被 punch,不可被发现)
    pub hidden: bool,
}
```
`do_connect` 内:
```rust
let reg = RelayMsg::Register {
    name: register_display_name(&config.device_name),
    fingerprint: identity.fingerprint(),
    hidden: config.hidden,
};
```

- [ ] **Step 2: 补三处构造点(编译驱动,读 config.hidden)**

FFI `relay_state.rs:34`(spawn_relay 开头元组):
```rust
let (enabled, server, psk, device_name, hidden) = {
    let config = state.config.read().await;
    (config.relay_enabled, config.relay_server.clone(), config.relay_psk.clone(), config.device_name.clone(), config.hidden)
};
```
`relay_state.rs:73`:
```rust
let relay_config = localtrans_core::relay::client::RelayClientConfig {
    server_addr,
    psk,
    device_name,
    hidden,
};
```

PC `main.rs:584-589`(启动段读 config 处加 hidden)+ `:603` 构造加 `hidden`;PC `commands.rs:1914`(device_name read 处一并读 hidden)+ `:1915` 构造加 `hidden: config_hidden`(局部变量名自定,来源必须是 `state.config` 的 `hidden` 字段)。

- [ ] **Step 3: 编译+回归**

```bash
cargo build -p localtrans-ffi -p localtrans-relay --release 2>&1 | tail -5
cargo test -p localtrans-ffi -- --test-threads=1
```
Expected: 编译通过、FFI 测试全绿。(字段直传无分支,上报语义由 Task 1 的 relay E2E `hidden_client_excluded_from_roster` 与装机验证覆盖)

- [ ] **Step 4: Commit**

```bash
git add crates/localtrans-core/src/relay/client.rs crates/localtrans-ffi/src/relay_state.rs src-tauri/src
git commit -m "fix(relay): RelayClient 注册携带隐身标志,双壳构造点透传 config.hidden

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 3: discovery 主动探测隐身门控(问题 01 本地半)

**Files:**
- Modify: `crates/localtrans-core/src/discovery.rs:608-627`(ProbeNow/ProbeAddr 命令分支)
- Test: `discovery.rs` tests(新 E2E)

**Interfaces:** 无新接口(行为门控)。

- [ ] **Step 1: 写失败测试**

`discovery.rs` tests 模块追加(端口避开现有 14760-14811 用例区间):

```rust
#[tokio::test]
async fn hidden_device_probes_nothing() {
    let (k1, k2) = (
        Arc::new(SigningKey::generate(&mut OsRng)),
        Arc::new(SigningKey::generate(&mut OsRng))
    );

    // B(正常)先启动:启动 burst 在 A 起来之前完成,不干扰断言
    let b = spawn(DiscoveryConfig {
        bind_port: 14831,
        target: "127.0.0.1:14830".parse().unwrap(),
        hidden: Default::default(),
        name: "乙".into(),
        quic_port: 24832,
        offline_secs: 15,
        data_dir: None,
        fingerprint: test_fp(&k2),
    }, k2.clone()).unwrap();
    tokio::time::sleep(Duration::from_millis(150)).await;

    // A(隐身)后启动:不发 burst、不周期广播
    let a = spawn(DiscoveryConfig {
        bind_port: 14830,
        target: "127.0.0.1:14831".parse().unwrap(),
        hidden: Arc::new(AtomicBool::new(true)),
        name: "甲".into(),
        quic_port: 24831,
        offline_secs: 15,
        data_dir: None,
        fingerprint: test_fp(&k1),
    }, k1.clone()).unwrap();

    // A 主动探测:应被 hidden 门控拦下 → B 无探测可应答 → A 设备表保持空。
    // 若门控缺失:B 回 ProbeResp → A 看到乙 → 断言失败(红)。
    a.cmd.send(DiscoveryCmd::ProbeNow).await.unwrap();
    tokio::time::sleep(Duration::from_millis(600)).await;

    let a_list = a.devices.borrow().clone();
    assert!(
        a_list.iter().all(|d| d.name != "乙"),
        "隐身设备发出的探测应被拦,A 不应通过探测应答看到乙(实得 {:?})",
        a_list
    );

    let _ = tokio::join!(a.shutdown.send(()), b.shutdown.send(()));
    tokio::time::sleep(Duration::from_millis(300)).await;
}
```

时序说明:B 启动→断言全程 <1.2s,远小于周期 Presence 下限 4s(`presence_period()` = 4000+rand(0..2000)ms),B 不会发周期广播;B 的 burst 已在 A 启动前发完(A 未监听,包丢)——A 设备表唯一可能来源是"B 对 A 探测的应答",正好是被测路径。

- [ ] **Step 2: 跑测试确认失败**

```bash
cargo test -p localtrans-core hidden_device_probes_nothing -- --test-threads=1
```
Expected: FAIL(A 通过探测应答看到了乙)。

- [ ] **Step 3: 实现门控**

`discovery.rs:608` 起两个分支各加守卫(`continue` 在 `while let Ok(cmd) = cmd_rx.try_recv()` 循环内,跳过本条命令继续排空):

```rust
DiscoveryCmd::ProbeNow => {
    if cfg.hidden.load(Ordering::Relaxed) {
        tracing::info!("隐身模式:跳过广播探测");
        continue;
    }
    let pkt = probe_packet(&cfg, &key, fingerprint, pubkey_bytes, next_own_nonce(&mut own_nonces));
    tracing::info!("发出广播探测 -> {}", cfg.target);
    if let Err(e) = socket.send_to(&pkt, cfg.target) {
        tracing::warn!("广播探测发送失败 -> {}: {}", cfg.target, e);
    }
}
DiscoveryCmd::ProbeAddr(addr) => {
    if cfg.hidden.load(Ordering::Relaxed) {
        tracing::info!("隐身模式:跳过单播探测 -> {}", addr);
        continue;
    }
    ...(原有发送+记入周期重探逻辑不变)
}
```

注意:启动 burst(`:564`)、周期 Presence(`:749`)、SetName 广播(`:638`)、探测应答(`:715-731`)四处已有 hidden 门控,不动。

- [ ] **Step 4: 跑测试转绿+回归**

```bash
cargo test -p localtrans-core -- --test-threads=1
```
Expected: 全绿(含既有 `hidden_device_not_listed_but_can_see`)。

- [ ] **Step 5: Commit**

```bash
git add crates/localtrans-core/src/discovery.rs
git commit -m "fix(discovery): 隐身设备不再发出主动探测(ProbeNow/ProbeAddr 门控)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 4: set_hidden 切换触发中继重注册(问题 01 双壳接线)

**Files:**
- Modify: `crates/localtrans-ffi/src/lib.rs:789-799`(set_hidden)
- Modify: `src-tauri/src/commands.rs:21-32`(set_hidden)、`commands.rs:1891-1979`(抽 reconnect_relay)

**Interfaces:**
- Consumes: Task 2 的 `RelayClientConfig.hidden` 与 FFI 已有 `spawn_relay`
- Produces: PC `pub async fn reconnect_relay(app: &AppHandle, state: &AppState, server: String, psk: String) -> Result<(), String>`(commands.rs,set_relay_config 与 set_hidden 共用)

- [ ] **Step 1: FFI set_hidden 感知重连**

`lib.rs:789` 替换为(注意先 drop config 写锁再 spawn_relay——后者内部要读 config,持锁调用会死锁):

```rust
/// Set hidden mode
pub fn set_hidden(&self, hidden: bool) {
    let state_guard = self.state.lock().unwrap();
    if let Some(state) = &*state_guard {
        let was_hidden = state.hidden.load(Ordering::Relaxed);
        state.hidden.store(hidden, Ordering::Relaxed);
        let relay_enabled = {
            let mut config = self.runtime.block_on(async { state.config.write().await });
            config.hidden = hidden;
            let dir = &state.dir;
            let _ = localtrans_core::store::save_config(dir, &config);
            config.relay_enabled
        };
        // 隐身翻转→中继重注册(Register.hidden 决定名册可见性);
        // spawn_relay 内部会读 config,上面的写锁已随块结束释放
        if relay_enabled && was_hidden != hidden {
            let state_ptr: &crate::state::AppState = state;
            let cb = self.callback.clone();
            self.runtime.block_on(async move {
                crate::relay_state::spawn_relay(state_ptr, &cb).await;
            });
        }
    }
}
```

- [ ] **Step 2: PC 抽 reconnect_relay + set_hidden 接线**

`commands.rs`:把 `set_relay_config` 1891 行起的"启用分支"整段(关旧连接→abort 旧任务→清名册→构造 RelayClientConfig(此时 `hidden` 取 `state.config.read().await.hidden`)→connect→事件桥→存句柄→emit Registered)搬入新函数:

```rust
/// 按 config 重建中继连接(set_relay_config 与 set_hidden 隐身翻转共用)
pub async fn reconnect_relay(
    app: &AppHandle,
    state: &AppState,
    server: String,
    psk: String,
) -> Result<(), String> {
    // ...(搬入的原 1891-1979 段,body 原样,仅 app_handle 改用入参 app)
}
```

`set_relay_config` 校验+落盘后调用 `reconnect_relay(&app, &state, server, psk).await`;禁用分支(1980 起)不变。

`set_hidden` 替换为:

```rust
/// 设置是否隐藏(隐身翻转时若中继已启用,重注册使名册可见性即时生效)
#[tauri::command]
pub async fn set_hidden(
    app: AppHandle,
    state: State<'_, AppState>,
    hidden: bool,
) -> Result<(), String> {
    let was_hidden = state.hidden.load(std::sync::atomic::Ordering::Relaxed);
    state.hidden.store(hidden, std::sync::atomic::Ordering::Relaxed);

    let (relay_enabled, server, psk) = {
        let mut config = state.config.write().await;
        config.hidden = hidden;
        let dir = &state.dir;
        localtrans_core::store::save_config(dir, &config).map_err(|e| e.to_string())?;
        (config.relay_enabled, config.relay_server.clone(), config.relay_psk.clone())
    };

    if relay_enabled && was_hidden != hidden {
        reconnect_relay(&app, &state, server, psk).await?;
    }
    Ok(())
}
```

前端 `invoke("set_hidden", { hidden })` 调用无需改(Tauri 自动注入 AppHandle/State)。

- [ ] **Step 3: 编译+回归**

```bash
cargo build --release -p localtrans-ffi 2>&1 | tail -3
cd src-tauri && cargo check 2>&1 | tail -3
cargo test -p localtrans-ffi -- --test-threads=1
```
Expected: 编译通过、测试全绿。(重连路径行为由 v0.9.0 既有 save_settings 重连机制同构保证+装机验证)

- [ ] **Step 4: Commit**

```bash
git add crates/localtrans-ffi/src/lib.rs src-tauri/src/commands.rs
git commit -m "fix(relay): 隐身开关翻转即时重注册中继——名册可见性不再滞后到重启

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 5: core 配对先回包后收尾(问题 04 时序根因)

**Files:**
- Modify: `crates/localtrans-core/src/session.rs:1256-1267`(Matched 分支)

**Interfaces:** 无(纯顺序调整,协议与事件不变)。

- [ ] **Step 1: 调整顺序**

`session.rs:1256` 的 Matched 分支改为——网络回包最先发出,对端(输码方)不因本地收尾耗时/连接竞争错过解除等待的信号:

```rust
PairOutcome::Matched => {
    terminal_result_sent = true;
    // 先回包再本地收尾:A 侧(输码方)依赖 PairResult 解除输码等待;
    // complete_pairing(写信任+SessionUp)耗时或连接竞争不应推迟回包
    let _ = send_all(&mut send, &ControlMsg::PairResult { ok: true }).await;
    if self.complete_pairing(&peer_fp, &conn).await {
        let _ = self.event_tx.send(SessionEvent::PairingResult {
            fingerprint: peer_fp,
            ok: true,
            reason: None,
        }).await;
    }
}
```

FailedOut/Mismatch 分支已是先回包,不动;1288 行起 `PairResult{ok:true}` 接收方(A 侧本地完成)不动。

- [ ] **Step 2: 回归(既有配对 E2E 断言双方 ok:true、到达顺序不限,正是本改动的护栏)**

```bash
cargo test -p localtrans-core session -- --test-threads=1
```
Expected: 全绿。

- [ ] **Step 3: Commit**

```bash
git add crates/localtrans-core/src/session.rs
git commit -m "fix(pairing): B 侧码匹配先回 PairResult 再本地收尾——输码方不再错过解除信号

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 6: PC 配对成功即时刷新设备卡片(问题 03)

**Files:**
- Modify: `src-tauri/src/main.rs:794-814`(PairingResult 分支)、`main.rs:815-827`(SessionUp 分支抽取共用)

**Interfaces:**
- Produces: `async fn emit_merged_device_list(app: &tauri::AppHandle, st: &AppState)`(main.rs 内自由函数)

- [ ] **Step 1: 抽公共函数**

main.rs 事件桥闭包外层附近新增:

```rust
/// 合并本地/名册/已连接并推送 device-list(SessionUp 与 PairingResult 共用)
async fn emit_merged_device_list(app: &tauri::AppHandle, st: &AppState) {
    let devices = st.devices.lock().await.clone();
    let roster = st.relay_roster.lock().await.clone();
    let connected = st.connected_fps.lock().await.clone();
    let aliases = crate::commands::alias_map(&*st.trust.lock().await);
    let dtos = crate::commands::merge_devices(&devices, &roster, &connected, &aliases);
    let _ = app.emit("device-list", dtos);
}
```

SessionUp 分支(main.rs:819-827)的重建段替换为:
```rust
if let Some(st) = app_handle_clone.try_state::<AppState>() {
    st.connected_fps.lock().await.insert(fp_hex.clone());
    emit_merged_device_list(&app_handle_clone, &*st).await;
}
```
(SessionUp 分支其余——connection-state emit、自动续传回路——不动。)

- [ ] **Step 2: PairingResult ok:true 双保险刷新**

`main.rs:794` 分支在移出 pending_pairing 之后、emit pairing-result 之前加:

```rust
if ok {
    // 双保险:SessionUp 迟到/丢失(中继内层握手竞争)时,
    // 配对成功事件本身完成 connected 徽章刷新
    if let Some(st) = app_handle_clone.try_state::<AppState>() {
        st.connected_fps.lock().await.insert(fp_hex.clone());
        emit_merged_device_list(&app_handle_clone, &*st).await;
    }
    let _ = app_handle_clone.emit("connection-state", serde_json::json!({
        "fingerprint": fp_hex,
        "up": true
    }));
}
```

(与 SessionUp 的重复 emit 幂等无害——同 fingerprint 二次 insert/同列表重推。)

- [ ] **Step 3: 编译验证**

```bash
cd src-tauri && cargo check 2>&1 | tail -3
```
Expected: 通过。(事件链时序无法单测,装机双机验证清单见 Task 11)

- [ ] **Step 4: Commit**

```bash
git add src-tauri/src/main.rs
git commit -m "fix(ui): 配对成功即时重推设备列表——SessionUp 迟到不再卡旧卡片状态

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 7: PC 两处 CSS 修复(问题 02/06)

**Files:**
- Modify: `ui/src/components/DeviceCard.vue:495`(min-width)、`:565`(select max-width)
- Modify: `ui/src/pages/Devices.vue:680-687`(.form-input)

- [ ] **Step 1: DeviceCard 下拉加宽**

`DeviceCard.vue:495`:`min-width: 230px;` → `min-width: 280px;`
`DeviceCard.vue:565`:`max-width: 110px;` → `max-width: 130px;`

- [ ] **Step 2: Devices 输入框 border-box**

`Devices.vue:680` 的 `.form-input` 规则加一行:

```css
.form-input {
  width: 100%;
  box-sizing: border-box;   /* 新增:padding 计入宽度,不再溢出弹窗 */
  padding: 10px var(--space-3);
  ...(其余不变)
}
```

- [ ] **Step 3: 前端构建验证**

```bash
cd ui && npm run build 2>&1 | tail -5
```
Expected: 构建成功。

- [ ] **Step 4: Commit**

```bash
git add ui/src/components/DeviceCard.vue ui/src/pages/Devices.vue
git commit -m "fix(ui): 设备卡片下拉菜单加宽至 280px;添加设备弹窗输入框 border-box 防溢出

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 8: Android 配对输码超时兜底 + 错误可见(问题 04 壳层半)

**Files:**
- Modify: `android/app/src/main/java/com/localtrans/app/ui/devices/DevicesUiModel.kt`(PairingUiState 加 deadlineEpochMs;DevicesUiState 加 consentTimeoutSecs/error)
- Modify: `android/app/src/main/java/com/localtrans/app/ui/devices/DevicesViewModel.kt`(PairingCodeEntry 设 deadline;ticker 超时转 FAILED;submit/connect 错误可见)
- Modify: `android/app/src/main/java/com/localtrans/app/ui/devices/DevicesScreen.kt`(error Snackbar)
- Test: `android/app/src/test/java/com/localtrans/app/ui/devices/DevicesViewModelTest.kt`(追加 2 用例;FakeDevicesRepo 加 submitError 注入)

**Interfaces:**
- Consumes: Task 5 的 core 时序修复(配对成功回包及时到达,超时兜底只覆盖真正的异常路径);**Task 9 的 Repo suspend 签名(本任务测试按 suspend Fake 书写,必须在 Task 9 之后执行)**
- Produces: `PairingUiState.deadlineEpochMs: Long`(默认 0=无期限);`DevicesUiState.consentTimeoutSecs: Long`(默认 60)、`error: String?`(默认 null)

- [ ] **Step 1: 写失败测试**

DevicesViewModelTest.kt 追加(沿用文件现有 Main dispatcher 测试基建;FakeDevicesRepo.settings() 返回 consentTimeoutSecs = 30u,故超时 30s):

```kotlin
@Test
fun `code entry times out to failed after consent timeout`() = runTest {
    val repo = FakeDevicesRepo()
    val vm = DevicesViewModel(repo)
    repo.emitEvent(AppEvent.PairingCodeEntry(fingerprint = "fp1", name = "PC"))
    assertEquals(PairingState.CODE_ENTRY, vm.uiState.value.pairing?.state)
    advanceTimeBy(31_000)
    assertEquals(PairingState.FAILED, vm.uiState.value.pairing?.state)
    assertEquals("等待对端确认超时", vm.uiState.value.pairing?.reason)
}

@Test
fun `submit code error surfaces as failed`() = runTest {
    val repo = FakeDevicesRepo()
    repo.submitError = RuntimeException("控制流已关闭")
    val vm = DevicesViewModel(repo)
    repo.emitEvent(AppEvent.PairingCodeEntry(fingerprint = "fp1", name = "PC"))
    vm.submitPairingCode("fp1", "123456")
    advanceUntilIdle()
    assertEquals(PairingState.FAILED, vm.uiState.value.pairing?.state)
    assertTrue(vm.uiState.value.pairing?.reason?.contains("控制流已关闭") == true)
}
```

FakeDevicesRepo.kt 加注入点(签名按 Task 9 之后的 suspend 形态):
```kotlin
var submitError: Exception? = null

override suspend fun submitPairingCode(fingerprint: String, code: String) {
    submitError?.let { throw it }
}
```

- [ ] **Step 2: 确认失败**

```bash
cmd //c "set JAVA_HOME=C:/Users/<user>\Desktop\work\deepseek_use\tools\jdk17\jdk-17.0.20+8&& C:/Users/<user>\Desktop\work\deepseek_use\tools\gradle-8.10.2\bin\gradle.bat -p C:/Users/<user>\Desktop\work\localTrans\android testDebugUnitTest --tests *DevicesViewModelTest*" > /tmp/g.log 2>&1; tail -20 /tmp/g.log
```
Expected: 新用例 FAIL(无 deadline 机制/错误被吞)。

- [ ] **Step 3: 实现**

DevicesUiModel.kt:
```kotlin
data class PairingUiState(
    val fingerprint: String,
    val peerName: String,
    val code: String = "",
    val state: PairingState,
    val reason: String = "",
    /** CODE_ENTRY 截止时刻(epoch ms;0=无期限) */
    val deadlineEpochMs: Long = 0L
)

data class DevicesUiState(
    val myFingerprint: String = "",
    val hidden: Boolean = false,
    val devices: List<DeviceUi> = emptyList(),
    val pairing: PairingUiState? = null,
    val loading: Boolean = false,
    val consentTimeoutSecs: Long = 60L,
    val error: String? = null
)
```

DevicesViewModel.kt:
- `loadSettings()` 存 `consentTimeoutSecs = settings.consentTimeoutSecs.toLong()`
- PairingCodeEntry 分支设 deadline:
```kotlin
is AppEvent.PairingCodeEntry -> {
    val timeoutMs = _uiState.value.consentTimeoutSecs * 1000L
    _uiState.update {
        it.copy(
            pairing = PairingUiState(
                fingerprint = event.fingerprint,
                peerName = event.name,
                state = PairingState.CODE_ENTRY,
                deadlineEpochMs = System.currentTimeMillis() + timeoutMs
            )
        )
    }
}
```
- init 末尾加超时看护(每秒一 tick,虚拟时间可推进):
```kotlin
viewModelScope.launch {
    while (true) {
        kotlinx.coroutines.delay(1000)
        val st = _uiState.value.pairing ?: continue
        if (st.state == PairingState.CODE_ENTRY &&
            st.deadlineEpochMs > 0 &&
            System.currentTimeMillis() >= st.deadlineEpochMs
        ) {
            _uiState.update {
                it.copy(pairing = st.copy(state = PairingState.FAILED, reason = "等待对端确认超时"))
            }
        }
    }
}
```
- `submitPairingCode` 错误可见:
```kotlin
fun submitPairingCode(fingerprint: String, code: String) {
    viewModelScope.launch {
        try {
            repo.submitPairingCode(fingerprint, code)
        } catch (e: Exception) {
            _uiState.update {
                it.copy(pairing = it.pairing?.copy(state = PairingState.FAILED, reason = e.message ?: "提交失败"))
            }
        }
    }
}
```
- `connectDevice` 错误可见(同构,catch 写 `_uiState.update { it.copy(error = e.message ?: "连接失败") }`)。

DevicesScreen.kt 顶部 Column 内加:
```kotlin
uiState.error?.let { msg ->
    Snackbar(
        modifier = Modifier.padding(8.dp),
        action = { TextButton(onClick = { /* error 为一次性快照,重组自然消除 */ }) { Text("知道了") } }
    ) { Text(msg) }
}
```
(展示性兜底,样式随现有 Snackbar 用法。)

- [ ] **Step 4: 跑测试转绿**

同 Step 2 命令。Expected: 新旧用例全绿。

- [ ] **Step 5: Commit**

```bash
git add android/app/src/main/java/com/localtrans/app/ui/devices android/app/src/test
git commit -m "fix(pairing): 输码弹窗超时兜底(复用同意超时配置)+提交/连接错误不再静默吞掉

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 9: Android Repo suspend 化——FFI 调用全部下 IO 线程(问题 05,先于 Task 8 执行)

**Files:**
- Modify: `android/app/src/main/java/com/localtrans/app/data/DevicesRepo.kt`(接口+Ffi+Fake)
- Modify: `android/app/src/main/java/com/localtrans/app/data/TransfersRepo.kt`(同)
- Modify: `android/app/src/main/java/com/localtrans/app/data/FilesRepo.kt`(同)
- Modify: `android/app/src/main/java/com/localtrans/app/data/SettingsRepo.kt`(同)
- Modify: `android/app/src/main/java/com/localtrans/app/ui/files/FilesScreen.kt:936,1022`(app.devices() 包 IO)
- Modify: 各 ViewModel 调用点核对(DevicesViewModel/TransfersViewModel/FilesViewModel/SettingsViewModel——方法签名变 suspend 后,协程内的调用零改动;非协程内的调用点包 `viewModelScope.launch`)

**Interfaces:**
- Produces: 四个 Repo 接口的全部方法由 `fun` 改 `suspend fun`(`val events: SharedFlow<AppEvent>` 属性不变);Ffi 实现体统一 `withContext(Dispatchers.IO) { app.xxx() }`;Fake 实现签名同步 suspend、方法体不变

**执行顺序:本任务必须先于 Task 8**(Task 8 的测试按 suspend 签名书写)。**调度注意:执行时先跑本任务(Task 9)再跑 Task 8**——编号不代表执行顺序。

- [ ] **Step 1: 接口与实现 suspend 化(以 DevicesRepo 为完整样例,其余三个文件同模式)**

DevicesRepo.kt——接口每个方法加 `suspend`:
```kotlin
interface DevicesRepo {
    suspend fun devices(): List<DeviceDto>
    suspend fun connectDevice(fingerprint: String)
    suspend fun disconnect(fingerprint: String)
    suspend fun respondConsent(fingerprint: String, accept: Boolean)
    suspend fun submitPairingCode(fingerprint: String, code: String)
    suspend fun cancelWait(fingerprint: String)
    suspend fun setHidden(hidden: Boolean)
    suspend fun myFingerprint(): String
    suspend fun settings(): SettingsDto
    val events: SharedFlow<AppEvent>
}
```

FfiDevicesRepo 实现体全部包 IO(FFI 内部多把锁+block_on,主线程直调是 ANR 根源):
```kotlin
class FfiDevicesRepo(
    private val app: uniffi.localtrans_ffi.LocalTransApp,
    eventFlow: SharedFlow<AppEvent>
) : DevicesRepo {

    override val events: SharedFlow<AppEvent> = eventFlow

    override suspend fun devices(): List<DeviceDto> =
        kotlinx.coroutines.withContext(kotlinx.coroutines.Dispatchers.IO) { app.devices() }

    override suspend fun connectDevice(fingerprint: String) =
        kotlinx.coroutines.withContext(kotlinx.coroutines.Dispatchers.IO) { app.connectDevice(fingerprint) }

    override suspend fun disconnect(fingerprint: String) =
        kotlinx.coroutines.withContext(kotlinx.coroutines.Dispatchers.IO) { app.disconnect(fingerprint) }

    override suspend fun respondConsent(fingerprint: String, accept: Boolean) =
        kotlinx.coroutines.withContext(kotlinx.coroutines.Dispatchers.IO) { app.respondConsent(fingerprint, accept) }

    override suspend fun submitPairingCode(fingerprint: String, code: String) =
        kotlinx.coroutines.withContext(kotlinx.coroutines.Dispatchers.IO) { app.submitPairingCode(fingerprint, code) }

    override suspend fun cancelWait(fingerprint: String) =
        kotlinx.coroutines.withContext(kotlinx.coroutines.Dispatchers.IO) { app.cancelWait(fingerprint) }

    override suspend fun setHidden(hidden: Boolean) =
        kotlinx.coroutines.withContext(kotlinx.coroutines.Dispatchers.IO) { app.setHidden(hidden) }

    override suspend fun myFingerprint(): String =
        kotlinx.coroutines.withContext(kotlinx.coroutines.Dispatchers.IO) { app.myFingerprint() }

    override suspend fun settings(): SettingsDto =
        kotlinx.coroutines.withContext(kotlinx.coroutines.Dispatchers.IO) { app.settings() }
}
```

FakeDevicesRepo:同签名改 `suspend fun`,方法体原样(不加 withContext——Fake 无阻塞)。返回 Unit 的写法注意 Kotlin 语法(`override suspend fun connectDevice(fingerprint: String) {}`)。

TransfersRepo/FilesRepo/SettingsRepo 三个文件:对接口内**每一个** fun 方法执行同样三步(接口加 suspend / Ffi 实现体包 withContext(Dispatchers.IO) / Fake 签名同步),方法清单以各文件当前定义为准(TransfersRepo 已知 8 方法:transfers/pause/resume/cancel/transferRemove/transfersClearFinished/retryTransfer/respondOffer)。

- [ ] **Step 2: ViewModel/Compose 调用点核对**

- DevicesViewModel/TransfersViewModel/SettingsViewModel/FilesViewModel:现有 repo 调用均在 `viewModelScope.launch { }` 内,suspend 化后零改动;若有编译暴露的非协程调用点,包 `viewModelScope.launch { ... }`。
- FilesScreen.kt:936 与 :1022 两处 Composable 内直调:
```kotlin
LaunchedEffect(Unit) {
    devices = try {
        kotlinx.coroutines.withContext(kotlinx.coroutines.Dispatchers.IO) { app.devices() }
    } catch (e: Exception) { emptyList() }
}
```
(:1022 同构。)

- [ ] **Step 3: 全量单测(编译暴露遗漏调用点+Fake 调用需协程内)**

```bash
cmd //c "set JAVA_HOME=C:/Users/<user>\Desktop\work\deepseek_use\tools\jdk17\jdk-17.0.20+8&& C:/Users/<user>\Desktop\work\deepseek_use\tools\gradle-8.10.2\bin\gradle.bat -p C:/Users/<user>\Desktop\work\localTrans\android testDebugUnitTest" > /tmp/g.log 2>&1; tail -30 /tmp/g.log
```
Expected: 全绿。测试中对 fake 方法的既有调用若在非协程上下文,包 `runTest { ... }`(机械修复,不改断言语义)。

- [ ] **Step 4: Commit**

```bash
git add android/app/src/main/java/com/localtrans/app android/app/src/test
git commit -m "fix(android): Repo 接口 suspend 化——FFI 调用全部移出主线程,消除 ANR 卡死根源

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 10: Android 长按菜单"发送到设备"(问题 07)

**Files:**
- Modify: `android/app/src/main/java/com/localtrans/app/ui/files/FileEntryMenuSheet.kt`(加 onSend 参数+本机分支菜单项)
- Modify: `android/app/src/main/java/com/localtrans/app/ui/files/FilesScreen.kt:552-583`(menuEntry 接线)
- Modify: `android/app/src/main/java/com/localtrans/app/ui/files/FilesViewModel.kt`(加 selectOnly)
- Modify: `android/app/src/main/java/com/localtrans/app/ui/files/MediaPickerViewModel.kt`(加 selectOnly)

**Interfaces:**
- Consumes: 现有 `handleSend(localTab, deviceFp, ...)` 按选中集发送、`DeviceSelectionSheet`、`onSendSuccess` 清选中
- Produces: `FileEntryMenuSheet(onSend: () -> Unit)` 新参数(远程调用方传 `{}`,不渲染);`FilesViewModel.selectOnly(path: String)`;`MediaPickerViewModel.selectOnly(path: String)`

- [ ] **Step 1: FileEntryMenuSheet 加发送项**

```kotlin
@OptIn(ExperimentalMaterial3Api::class)
@Composable
fun FileEntryMenuSheet(
    isRemote: Boolean,
    onDismiss: () -> Unit,
    onDownload: () -> Unit,
    onSend: () -> Unit,
    onRename: () -> Unit,
    onDelete: () -> Unit,
    onSelectMultiple: () -> Unit
) {
    ModalBottomSheet(onDismissRequest = onDismiss) {
        if (isRemote) {
            MenuRow(icon = Icons.Default.Download, label = "下载到本机", onClick = onDownload)
        } else {
            MenuRow(icon = Icons.Default.Send, label = "发送到设备", onClick = onSend)
            MenuRow(icon = Icons.Default.Edit, label = "重命名", onClick = onRename)
            MenuRow(icon = Icons.Default.Delete, label = "删除", onClick = onDelete)
        }
        MenuRow(icon = Icons.Default.Checklist, label = "选择多项", onClick = onSelectMultiple)
        Spacer(modifier = Modifier.height(24.dp))
    }
}
```

- [ ] **Step 2: ViewModel 加 selectOnly**

FilesViewModel(字段名以文件实际为准,selectedEntries 的 MutableStateFlow):
```kotlin
/** 单文件直发用:清空现有选中后仅选中 path */
fun selectOnly(path: String) {
    _selectedEntries.value = setOf(path)
}
```
MediaPickerViewModel 同构(选中集为 id 集时,内部按 path 反查 id;若其选中集直接是 path/id 均以现有 selectedPaths() 数据流为准实现等价语义)。

- [ ] **Step 3: FilesScreen 接线**

`FilesScreen.kt:555` 的 FileEntryMenuSheet 调用加 onSend(远程页传空实现):

```kotlin
FileEntryMenuSheet(
    isRemote = isRemotePage,
    onDismiss = { menuEntry = null },
    onDownload = { ...(原有不动) },
    onSend = {
        val path = entry.path
        menuEntry = null
        when (localTab) {
                            LocalTab.PHOTOS, LocalTab.VIDEOS -> {
                mediaViewModel.selectOnly(path)
                showDeviceSheet = true
            }
            LocalTab.DOCS -> {
                docsSelected = setOf(path)
                showDeviceSheet = true
            }
            LocalTab.ALL -> {
                filesViewModel.selectOnly(path)
                showDeviceSheet = true
            }
        }
    },
    onRename = { ...(原有不动) },
    onDelete = { ...(原有不动) },
    onSelectMultiple = { ...(原有不动) }
)
```

后续流复用现有链:DeviceSelectionSheet 选设备 → `handleSend(localTab, fp, ...)` 按当前 Tab 选中集(此时已是单选)发送 → `onSendSuccess()` 清选中+Snackbar。

- [ ] **Step 4: 编译+单测+装机冒烟**

```bash
cmd //c "set JAVA_HOME=C:/Users/<user>\Desktop\work\deepseek_use\tools\jdk17\jdk-17.0.20+8&& C:/Users/<user>\Desktop\work\deepseek_use\tools\gradle-8.10.2\bin\gradle.bat -p C:/Users/<user>\Desktop\work\localTrans\android testDebugUnitTest assembleDebug" > /tmp/g.log 2>&1; tail -20 /tmp/g.log
```
Expected: 全绿+APK 构建成功。装机冒烟:四 Tab 各长按一个文件 → 菜单含"发送到设备" → 点击弹设备选择 → 选设备 → 传输页出现任务;远程页长按菜单不含发送。

- [ ] **Step 5: Commit**

```bash
git add android/app/src/main/java/com/localtrans/app/ui/files
git commit -m "feat(android): 本机文件长按菜单新增发送到设备——单文件三步并一步

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 11: v0.10.2 收尾(版本/CHANGELOG/全测/打包/tag)

**Files:**
- Modify: `Cargo.toml:7`(workspace version)、`src-tauri/Cargo.toml:3`、`src-tauri/tauri.conf.json:4`、`android/app/build.gradle.kts:23-24`、`CHANGELOG.md`

- [ ] **Step 1: 版本四处对齐**

- `Cargo.toml`:`version = "0.10.1"` → `"0.10.2"`
- `src-tauri/Cargo.toml`:`version = "0.9.2"` → `"0.10.2"`(历史欠账本次对齐)
- `src-tauri/tauri.conf.json`:`"version": "0.10.1"` → `"0.10.2"`
- `android/app/build.gradle.kts`:`versionCode = 12` → `13`;`versionName = "0.10.1"` → `"0.10.2"`

- [ ] **Step 2: CHANGELOG 条目**

```markdown
## [0.10.2] - 2026-08-25

### 修复
- **隐身模式真正隐身**:中继 Register 上报隐身标志、服务端名册剔除隐身设备(半隐身:自己可见名册、可主动连接,别人发现不了你);隐身时不再发出主动探测;隐身开关翻转即时重注册中继(PC+Android)
- **Android ANR 卡死**:Repo 接口 suspend 化,FFI 调用(内部多锁+block_on)全部移出主线程
- **配对输码弹窗停留**:B 侧码匹配先回 PairResult 再本地收尾(输码方不再错过解除信号);Android 输码弹窗超时兜底(复用同意超时配置)+提交/连接错误不再静默
- **PC 配对成功卡片滞后**:配对成功事件即时重推设备列表(SessionUp 迟到不再卡旧状态)
- **PC UI**:设备卡片三点菜单加宽(230→280px,select 110→130px);手动添加设备弹窗输入框 border-box 防溢出

### 新增
- Android 本机文件长按菜单"发送到设备"(四 Tab 同构,单文件三步并一步)
```

- [ ] **Step 3: 全量测试**

```bash
cargo test -p localtrans-core -p localtrans-ffi -p localtrans-relay -- --test-threads=1
cmd //c "set JAVA_HOME=C:/Users/<user>\Desktop\work\deepseek_use\tools\jdk17\jdk-17.0.20+8&& C:/Users/<user>\Desktop\work\deepseek_use\tools\gradle-8.10.2\bin\gradle.bat -p C:/Users/<user>\Desktop\work\localTrans\android testDebugUnitTest" > /tmp/g.log 2>&1; tail -10 /tmp/g.log
```
Expected: 全绿。

- [ ] **Step 4: 双端产物+dist**

- PC release 重编(cargo build --release -p localtrans 桌面主程序)、WSL Ubuntu relay tar.gz 重打(01 改了 relay 服务端,**中继必须同版部署**)
- Android:so 重编(cargo ndk)+ `assembleRelease` + aapt 验证 versionCode='13' versionName='0.10.2' + apksigner 验签
- dist 组装:zip 隐私核查(**不得含 data/ 目录**;PSK/私钥不入产物)

- [ ] **Step 5: Commit+tag**

```bash
git add Cargo.toml src-tauri/Cargo.toml src-tauri/tauri.conf.json android/app/build.gradle.kts CHANGELOG.md
git commit -m "chore: v0.10.2 收尾——版本对齐/CHANGELOG/双端产物

Co-Authored-By: Claude <noreply@anthropic.com>"
git tag v0.10.2
```

- [ ] **Step 6: 装机双机验证清单(人工)**

1. 隐身:PC 开隐身→手机设备列表(局域网+中继两场景)看不到 PC;关隐身 5s 内恢复可见;隐身期间 PC 能主动连手机
2. 配对:手机发起连接输码→PC 显示配对成功的同时手机弹窗应立即转"配对成功"(≤1s);人为断网重试,输码弹窗 60s(或设置值)超时转"配对失败"
3. 卡死:传输大文件期间高频切换设备页/文件页/传输页,无 ANR、无冻结
4. 长按发送:四 Tab 长按→发送到设备→选设备→传输页出现任务
5. PC:三点菜单文案完整显示;手动添加设备弹窗输入框不溢出;配对成功瞬间卡片徽章即变"已连接"
