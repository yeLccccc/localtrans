# LocalTrans 设计哲学

> LocalTrans 是一个局域网优先的文件传输工具：Rust 核心 + Tauri 桌面壳 + Kotlin 安卓壳，
> QUIC 传输、断点续传、配对权限管控，无云服务、无账号、无遥测。
> 本文记录贯穿整个代码库的设计取舍——每一条都能在代码里找到对应实现。

## 1. 本地优先：隐私是默认状态，不是选项

- 没有云端账户、没有遥测上报、没有"匿名统计"。两台设备之间的所有数据
  （设备发现、配对、传输）都不经过任何第三方服务器。
- 可选的中继服务器是一个**盲转发器**：数据面只转发不透明的 UDP 载荷，
  内层是两端之间的 mTLS 会话——中继运营者（包括我们自己部署的）看不到
  文件名、大小或内容；名册/租约全部内存态，进程重启即消失，落盘为零。
  客户端配置入口与自部署服务端均已开放。
- 日志只落本机 `data/logs/`；错误提示刻意不回显远端文件名（见
  `crates/localtrans-core/src/transfer/mod.rs` 的净化错误路径）。

## 2. 身份即密钥：配对是唯一的信任根

- 每台设备一把 Ed25519 身份密钥，**指纹（证书 DER 的 SHA-256）是全系统
  唯一的身份标识**——发现广播、TLS 钉扎、信任表、中继名册用的是同一个值。
- 配对采用**带外 SAS 模式**：6 位配对码只显示在被连接方的屏幕上，从不在
  网络上传输；发起方肉眼核对后输入。哈希常量时间比较，错 3 次锁 300 秒，
  且每次尝试都需要对端人工点"同意"——在线爆破在数学上可行、在流程上不可行。
- 配对之后，**每一次连接**都做证书指纹钉扎（mTLS）：即使攻击者在配对之后
  进入同一网络发起中间人，也无法出示与信任表一致的证书。
- 发现广播虽然明文，但每包带 Ed25519 签名 + nonce 防重放——可以伪造"一台
  新设备"，不能冒充"别人的设备"；而在配对之前，什么都不该被信任。

## 3. 失败关闭（fail-closed）

- 权限在**接收侧/服务侧**强制执行，而非依赖对端自觉：浏览、下载、推送
  三类权限按指纹逐台配置，未配对设备默认全拒。
- 推送的默认策略是"每次询问"，超时即拒绝；"自动接受"是逐台显式开启的。
- 远端提供的每一个文件名、每一个目录分量都要过净化器：`..\`、绝对路径、
  盘符、Windows 保留名、控制字符与双向覆盖字符全部拒绝或剥离——
  接收目录就是接收方的信任边界。

## 4. 安全不牺牲体验：软件从不主动打扰

安全机制如果让用户烦，就会被绕开。所以：

- 断线后的自动重连是**静默的**：整个过程只有两条提示——"重连成功"与
  "最终放弃"（连接记忆 + 指数退避 + 5 败回落，见 `docs/connect-memory.md`）。
- 配对与推送确认都是一次性弹窗 + 倒计时自动拒绝，不堆通知。
- Windows 防火墙规则提供一键添加（UAC 提权），同时给出手动命令——
  自动化失败时用户永远有出路。

## 5. 性能是特性：用测量说话，不用感觉说话

- 传输层选 QUIC（quinn），并直面它的代价：1200B MTU 让每个数据报负载
  很小。README 里的基准给出了物理论证——Windows 回环裸 UDP 1200B
  数据报天花板约 166 MB/s，引擎实测约 206 MB/s（多流并发后的报文聚合），
  剩余差距属于操作系统数据报路径，不是应用层浪费。
- 自适应流控按网络状况动态调整并发流数；验收口径绑定 **iperf3 基准的
  相对值**（≥90%），而不是一个拍脑袋的绝对数字。
- 断点续传按分片记账，中断后从中断处继续，不做"重新传一遍"的假续传。

## 6. 少的移动部件：架构简单的可负担性

- Rust workspace 四个成员职责单一：`localtrans-core`（协议与引擎，不碰
  UI）、`localtrans-relay`（可独立部署的中继）、`localtrans-ffi`（安卓
  JNI 壳）、`src-tauri`（桌面壳）。core 不知道壳的存在，壳只做编排。
- 桌面端单文件便携版，数据全部在 exe 旁的 `data/` 目录——拷走即迁移，
  不写注册表、不留全局状态。
- 中继是一个静态链接的 Linux 二进制 + systemd 单元，配置只有一个 TOML。

## 7. 可测试性是一等公民

- **纯函数下沉 core，编排留在壳层**：退避序列、抖动、文件名净化、配对
  状态机全部是 core 里的可测纯函数；壳层只管调度与 UI 时机。
- 泳道门禁：core / shell / relay / ui 四条泳道各有明确的测试命令
  （`scripts/gate.sh`），改哪条泳道跑哪条门禁，合并前集成者跑全量。
- 测试 API 三重门控（cargo feature + 编译期 `cfg` + 运行期环境变量），
  让 E2E 验收能驱动真实 UI，同时保证发布构建零负担、零攻击面。
- E2E 用真机矩阵（双 PC + 安卓真机）做字节级断言，验收要求截图目检
  渲染正确性——"后台断言通过"不等于"用户看到的是对的"。

## 8. 发布纪律

- 版本号四处同步（workspace / Tauri / Gradle / 配置），漏一处就是事故，
  所以发布手册里给了一行 grep 自检。
- 出包前的敏感信息门禁：真实 IP、PSK、密码、个人路径一律不得进入产物，
  示例文档使用文档保留段地址（203.0.113.x / 198.51.100.x）。
- 产物统一目录布局 + 变更日志随包，用户拿到的是一个自解释的文件夹，
  而不是一堆裸文件。

---

**一句话总结**：把"两台设备之间的直传"做到足够快、足够安全、足够省心——
加密与权限是默认而不是开关，重连与续传是自动而不是手动，
性能与正确性靠测量而不是感觉，发布靠纪律而不是运气。
